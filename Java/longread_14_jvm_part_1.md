# Введение

Java Virtual Machine (JVM) — это основа исполнения Java-программ. Она обеспечивает переносимость байткода между платформами, безопасность исполнения программы, управление памятью, оптимизации выполнения и поддержку множества JVM-языков.

Хотя разработчик пишет код на Java, именно JVM определяет, как этот код будет загружен, верифицирован, скомпилирован JIT-компилятором, размещён в памяти и выполнен. Понимание механизмов JVM необходимо для анализа производительности, диагностики ошибок и корректной работы приложений.

В этой лекции мы разберём ключевые компоненты JVM, путь исполнения кода, подсистему загрузки классов, этапы линковки, структуру областей памяти, виды ссылок на объекты и базовые принципы сборки мусора.

# 1. Понятия JDK / JRE / JVM / JLS / JVM Specification. Примеры реализаций JVM

В экосистеме Java существует несколько базовых терминов, которые определяют: язык, его спецификацию, виртуальную машину, среду исполнения и набор инструментов разработчика. Эти сущности относятся к разным частям платформы Java и выполняют разные задачи.

---

## 1.1. JVM — Java Virtual Machine

**JVM** — виртуальная машина, которая исполняет Java-байткод. Основные задачи JVM:

- выполнение инструкций байткода
- управление памятью, включая работу сборщика мусора
- обеспечение переносимости — байткод запускается одинаково на любой платформе с совместимой JVM
- обеспечение безопасности исполнения программы

JVM является **реализацией спецификации**, а не самостоятельным продуктом; существует несколько JVM, полностью соответствующих стандарту.

---

## 1.2. JRE — Java Runtime Environment

**JRE** — среда выполнения Java-программ. Включает:

- JVM,
- стандартные библиотеки Java SE.

JRE не содержит средств разработки, она предназначена только для запуска приложений.

> Важно отметить, что начиная с версии 11 Java не поставляется отдельно, только как часть JDK.

---

## 1.3. JDK — Java Development Kit

**JDK** — комплект разработчика. Включает:

- JRE,
- компилятор (`javac`),
- отладчик (`jdb`),
- инструменты упаковки (`jar`),
- дополнительные утилиты и инструменты профилирования.

JDK необходим для разработки, сборки и отладки Java-кода.

---

## 1.4. JLS — Java Language Specification

**JLS** определяет правила языка Java:

- синтаксис и семантику
- правила работы системы типов
- принципы разрешения перегрузок
- формальное описание поведения программ

JLS описывает **язык**, а не JVM.

> 🔗 Java Language Specification, SE 25: https://docs.oracle.com/javase/specs/jls/se25/html/index.html

---

## 1.5. JVM Specification

**JVM Specification** определяет:

- формат и структуру class-файлов
- набор инструкций байткода
- правила исполнения инструкций
- модель памяти JVM
- требования к подсистеме загрузки классов и сборке мусора

Если JLS описывает язык, то JVM Spec описывает исполняемую модель — как именно JVM должна выполнять байткод.

> 🔗 Java Virtual Machine Specification, SE 25: https://docs.oracle.com/javase/specs/jvms/se25/html/index.html

---

## 1.6. Реализации JVM

Существует несколько реализаций спецификации JVM.

### HotSpot (Oracle / OpenJDK)

Наиболее распространённая JVM. Особенности:

- JIT-компиляция
- исторически существовали отдельные режимы исполнения: Client VM (быстрый старт) и Server VM (агрессивные оптимизации). На современных 64‑битных платформах используется единая VM с многоуровневой (tiered) компиляцией по умолчанию
- используется по умолчанию в Oracle JDK и OpenJDK

### GraalVM (Oracle)

- основана на HotSpot, но использует Graal-компилятор
- поддерживает AOT (ahead-of-time compilation)
- применяется для высокопроизводительных и полиглотных сценариев
- позволяет создавать нативные образы (`native-image`)

### OpenJ9 (Eclipse Foundation)

- сниженное потребление памяти
- быстрый старт приложений
- часто используется в облачных и контейнерных окружениях

---

<details>
<summary><strong>📘 Вопросы для самопроверки</strong></summary>

1. Чем отличаются JDK, JRE и JVM?
2. Что описывает JLS и чем она отличается от JVM Specification?
3. В чём преимущества GraalVM по сравнению с HotSpot?
4. Почему OpenJ9 часто выбирают для сред с ограниченными ресурсами?

</details>

# 2. Путь исполнения кода

Исполнение Java-программы проходит несколько стандартных этапов — от исходного `.java` файла до выполнения байткода внутри JVM. Каждый шаг выполняет строго определённую задачу.

Полный цикл:

```
Compilation → Loading → Linking → Initialization → Execution
```

---

## 2.1. Compilation (компиляция)

Исходный файл:

```
MyClass.java
```

компилируется в байткод:

```
MyClass.class
```

Файл `.class` содержит:

- инструкции байткода
- пул констант
- метаданные о методах, полях и структуре класса

Этот шаг выполняется компилятором `javac`.

---

## 2.2. Loading (загрузка)

На этапе загрузки JVM:

- определяет, какой именно класс требуется программе
- по имени вида `com.example.MyClass` находит соответствующий `.class` файл
- загружает `.class` в память JVM. (Источником файла может быть например диск или сеть)

Результат загрузки — появление в JVM внутреннего представления класса.

---

## 2.3. Linking (линковка)

Линковка подготавливает класс к исполнению и включает:

- **verification** — проверку корректности структуры class-файла и байткода
- **preparation** — создание статических полей и присвоение им значений по умолчанию
- **resolution** — преобразование ссылок на другие сущности (классы, поля, методы) в конкретные внутренние структуры JVM

Процесс выполняется автоматически и прозрачен для разработчика.

---

## 2.4. Initialization (инициализация)

На этапе инициализации JVM:

- выполняет статические блоки (`static { ... }`)
- присваивает статическим полям **явные** значения, указанные в коде

Инициализация класса происходит один раз — при первом использовании.

---

## 2.5. Execution (исполнение)

На этом этапе начинается выполнение байткода:

- JVM вызывает методы программы
- создаются объекты
- запускаются алгоритмы, логика приложения, работа потоков и т. д

Это этап фактического исполнения программы после полной подготовки класса.

---

<details>
<summary><strong>📘 Вопросы для самопроверки</strong></summary>

1. Чем отличаются этапы Loading и Linking?
2. Когда статические поля получают значения по умолчанию, а когда — явные значения?
3. На каком этапе выполняются статические блоки?
4. Почему инициализация класса происходит только один раз?
5. Что является результатом этапа Compilation?

</details>

# 3. Компоненты JVM

JVM состоит из нескольких ключевых подсистем, которые определяют, как загружаются классы, где хранятся данные во время выполнения, как исполняется байткод и как происходит автоматическое освобождение памяти.

---

## 3.1. ClassLoader System

**ClassLoader System** отвечает за загрузку `.class` файлов в память JVM:

- находит класс по его полному имени (`com.example.MyClass`)
- читает соответствующий `.class` файл
- передаёт байткод JVM для создания внутреннего объекта `Class`

> ClassLoader **не выполняет линковку или инициализацию** — эти этапы полностью выполняет JVM после завершения загрузки.

---

## 3.2. Runtime Data Areas

**Runtime Data Areas** — это набор областей памяти, которые JVM создаёт для хранения данных программы во время исполнения. Они включают память для объектов, стеки потоков, метаданные классов и служебные структуры.

Каждая область имеет своё назначение и режим доступа (общая или отдельная для каждого потока). Это архитектурная основа JVM, определяющая организацию и модель памяти. Подробное устройство этих областей будет рассмотрено в пункте 6.

---

## 3.3. Execution Engine

**Execution Engine** отвечает за выполнение байткода. Он включает:

- интерпретатор, который выполняет инструкции последовательно
- JIT-компилятор, который преобразует часто выполняемые фрагменты байткода в машинный код
- механизмы оптимизации, использующие профиль исполнения

Execution Engine определяет производительность и эффективность выполнения Java-программ.

---

## 3.4. Garbage Collector

**Garbage Collector (GC)** автоматически управляет памятью кучи. Его задачи:

- определение недостижимых объектов
- освобождение занятой ими памяти
- минимизация фрагментации и оптимизация STW-паузы

GC работает прозрачно для разработчика и позволяет Java-программе обходиться без ручного освобождения памяти.

---

<details>
<summary><strong>📘 Вопросы для самопроверки</strong></summary>

1. Какие функции выполняет ClassLoader System и какие не входят в его обязанности?
2. Почему Runtime Data Areas являются ключевой архитектурной частью JVM?
3. Какую роль GC играет в стабильности и производительности Java-приложений?

</details>

# 4. Загрузка классов

Загрузка классов — это процесс, в котором JVM находит `.class` файл и создаёт в памяти внутреннее представление типа (`Class<?>`). Подсистема загрузчиков работает по модели делегирования: каждый ClassLoader передаёт попытку загрузки родителю, прежде чем загрузить класс самостоятельно.

---

## 4.1. Иерархия загрузчиков классов

### **4.1.1. Bootstrap ClassLoader**
- реализован на уровне нативного кода JVM
- загружает классы из модуля **`java.base`** (`java.lang`, `java.util`, `java.io`, и др.)
- не является Java-объектом (в API отображается как `null`)

### **4.1.2. Platform ClassLoader**
- загружает **Java SE Platform APIs** (например, `java.sql`, `java.net.http`)
- доступен через `ClassLoader.getPlatformClassLoader()`

### **4.1.3. Application ClassLoader**
- загружает классы приложения из classpath
- работает с JAR-файлами и зависимостями
- используется по умолчанию для пользовательского кода
- доступен через C`lassLoader.getSystemClassLoader()`

### **4.1.4. Custom ClassLoader**
Пользовательские загрузчики позволяют:

- загружать классы нестандартным способом (из сети, БД, шифрованных источников)
- реализовывать изоляцию модулей и различных плагинов
- подменять или фильтровать классы

---

## 4.2. Пример CustomClassLoader

```java
class ScratchClassLoader extends ClassLoader {
    @Override
    public Class<?> loadClass(String name) throws ClassNotFoundException {
        if (!name.equals("Scratch")) {
            return super.loadClass(name); // делегируем стандартной загрузке
        }
        try (var inputStream = Scratch.class.getResourceAsStream("Scratch.class")) {
            byte[] bytes = inputStream.readAllBytes();
            return defineClass("Scratch", bytes, 0, bytes.length);
        } catch (IOException e) {
            throw new ClassNotFoundException("failed");
        }
    }
}
```

Ниже — класс `Scratch` и `main`, демонстрирующие загрузку одним и разными загрузчиками.

```java
public class Scratch implements Runnable {

    @Override
    public void run() {
        System.out.println("Scratch::run");
    }

    public static void main(String[] args) throws ReflectiveOperationException {
        var scratchClassLoader = new ScratchClassLoader();

        Class<?> customLoaded = scratchClassLoader.loadClass("Scratch");
        Runnable loaded = customLoaded.asSubclass(Runnable.class)
                                      .getConstructor()
                                      .newInstance();

        Runnable instance = new Scratch();

        loaded.run();   // Scratch::run
        instance.run(); // Scratch::run

        System.out.println(instance.getClass());
        System.out.println(loaded.getClass());

        System.out.println(instance.getClass().getClassLoader()); // jdk.internal.loader.ClassLoaders$AppClassLoader@

        System.out.println(loaded.getClass().getClassLoader()); // ScratchClassLoader@
    }
}
```

Вывод показывает, что классы имеют одинаковое имя, но загружены разными ClassLoader’ами.

---

## 4.3. Почему одинаковые классы, загруженные разными ClassLoader’ами — разные типы

JVM определяет тип по паре: полное имя класса, загрузчик.

Поэтому два класса `Scratch` с одинаковым байткодом, но загруженные разными загрузчиками, считаются разными типами. Это приводит к ошибке при приведении типов.

```java
public class Scratch implements Runnable {

    @Override
    public void run() {
        System.out.println("Scratch::run");
    }

    public static void main(String[] args) throws ReflectiveOperationException {
        var scratchClassLoader = new ScratchClassLoader();

        Class<?> customLoaded = scratchClassLoader.loadClass("Scratch");
        Runnable loaded = customLoaded.asSubclass(Runnable.class)
                                      .getConstructor()
                                      .newInstance();

        Scratch casted = (Scratch) loaded; // <-- ClassCastException
    }
}
```

Ошибка выполнения:

```
Exception in thread "main" java.lang.ClassCastException: class Scratch cannot be cast to class Scratch (Scratch is in unnamed module of loader ScratchClassLoader@...; Scratch is in unnamed module of loader 'app')
```

Это наглядно демонстрирует, что загрузчик — часть идентичности типа.

---

<details>
<summary><strong>📘 Вопросы для самопроверки</strong></summary>

1. Какие загрузчики входят в стандартную иерархию JVM?
2. Что загружает Platform ClassLoader?
3. Для чего используется CustomClassLoader?
4. Почему один и тот же `.class` файл, загруженный разными загрузчиками, превращается в разные типы?
5. Что демонстрирует пример с `ScratchClassLoader` и почему возникает ClassCastException?

</details>

# 5. Linking: verification, preparation, resolution

После загрузки класса JVM выполняет этап *линковки*, подготавливая класс к использованию. Линковка преобразует загруженный байткод в структуру, готовую для выполнения, и включает три подэтапа.

---

## 5.1. Verification (верификация)

На этом этапе JVM проверяет корректность class-файла и байткода. Основные цели:

- структура class-файла соответствует спецификации JVM
- инструкции байткода типобезопасны и корректны с точки зрения стековой модели
- ссылки на другие сущности синтаксически валидны

При нарушениях JVM выбрасывает **VerifyError**.

---

## 5.2. Preparation (подготовка)

На этапе подготовки JVM:

- создаёт статические поля класса в памяти
- присваивает им **значения по умолчанию** (`0`, `null`, `false` и т. п.)

На этом шаге **не выполняется пользовательский код** — только выделение памяти и базовая инициализация.

---

## 5.3. Resolution (разрешение ссылок)

JVM преобразует *символьные ссылки* (текстовые ссылки внутри class-файла) в *прямые ссылки* на реальные структуры в памяти:

- ссылка на имя класса → объект `Class`
- ссылка на имя метода → конкретный метод, который должен быть вызван при выполнении
- ссылка на имя поля → конкретное поле, доступное во время выполнения

Особенности Resolution:

- разрешение может выполняться **лениво**, при первом обращении к методу, полю или классу
- если требуемый элемент отсутствует, JVM выбрасывает ошибки:  
  **NoClassDefFoundError**, **NoSuchMethodError**, **NoSuchFieldError**.

---

<details>
<summary><strong>📘 Вопросы для самопроверки</strong></summary>

1. Зачем JVM выполняет этап Verification?
2. Как вы думаете, почему на этапе Preparation не выполняется пользовательский код?
3. Какие ошибки могут возникнуть при Resolution и почему?

</details>

# 6. Runtime Data Areas

Runtime Data Areas — это набор областей памяти, которые JVM создаёт при запуске и использует на протяжении исполнения программы. Области делятся на общие (shared) и отдельные для каждого потока (per-thread) и определяют архитектуру памяти JVM.

---

## 6.1. Heap

**Heap** — основная область памяти, в которой размещаются все объекты приложения. Характеристики:

- общая для всех потоков
- управляется сборщиком мусора
- разделена на две генерации: **Young Generation** (короткоживущие объекты) и **Old Generation** (долгоживущие объекты)

---

## 6.2. Stack

Каждый поток имеет собственный **Java Stack**, содержащий:

- stack frames (один frame на вызов метода)
- локальные переменные метода
- операндный стек (используется для выполнения байткода)
- возвращаемые значения

Stack не хранит сами объекты — только примитивы и ссылки на объекты в Heap.

---

## 6.3. Metaspace

**Metaspace** — область нативной памяти, в которой JVM хранит метаданные классов: описание структуры класса, методы, поля, сигнатуры, данные пула констант.

Особенности:

- заменяет PermGen начиная с Java 8
- может расширяться динамически
- используется для структурного описания типов, а не для размещения объектов

---

## 6.4. Code Cache

**Code Cache** — область, в которой JIT-компилятор размещает сгенерированный машинный код. Это позволяет JVM ускорять работу нагруженных или часто вызываемых методов (так называемых «горячих» методов).

---

## 6.5. PC Register (Program Counter Register)

Каждый поток имеет собственный **PC-регистр**, который указывает на следующую инструкцию байткода, подлежащую выполнению. Если поток выполняет нативный метод, значение PC-регистра считается «неопределённым» (undefined).

---

## 6.6. Native Memory

JVM использует и **native memory**, которая не относится к Heap. В ней могут размещаться:

- внутренние структуры JVM
- данные JIT-компиляции
- буферы и вспомогательные структуры
- память JNI (Java Native Interface — механизм вызова нативного кода из Java)
- области Metaspace и Code Cache

---

## 6.7. Thread-Local Allocation Buffers (TLAB)

TLAB — это часть Heap (в Young Generation), выделяемая JVM каждому потоку.

Зачем они нужны:

- создание объектов — одна из самых частых операций в Java
- без TLAB выделение памяти требовало бы синхронизации между потоками
- TLAB позволяет потоку выполнять аллокацию автономно, без блокировок

Результат — значительное ускорение процесса аллокации памяти и, как следствие, ускоренное создание объектов.

---

## 6.8. Object Header

Каждый объект в Heap содержит **заголовок (object header)** — служебную информацию, необходимую JVM.

Обычно он включает:

- **mark word** — служебные данные: хэш-код, состояние блокировки, данные GC
- **class reference** — ссылка на мета-информацию о классе в Metaspace
- **alignment padding** — выравнивание объекта по границе (обычно до 8 байт)

> На 64-битных JVM часто включена оптимизация **compressed oops**, позволяющая хранить ссылки в 4 байтах вместо 8 и тем самым уменьшать объём используемой памяти.

---

<details>
<summary><strong>📘 Вопросы для самопроверки</strong></summary>

1. Чем Heap отличается от Stack и почему их разделение важно?
2. Что хранится в Metaspace, а что в Heap?
3. Зачем нужны TLAB и как они ускоряют работу программы?
4. Что такое mark word и какую информацию JVM хранит в нём?
5. Зачем каждому потоку отдельный PC-регистр?

</details>

# 7. Garbage Collection (GC)

Garbage Collector — это подсистема JVM, автоматически освобождающая память, занятую объектами, **которые более не используются программой**. GC является ключевым элементом модели памяти Java и позволяет разработчику **полностью избавиться от ручного управления освобождением памяти**.

GC работает на основе нескольких фундаментальных алгоритмов и имеет разные реализации, оптимизированные под разные сценарии нагрузки.

---

## 7.1. Базовые алгоритмы GC

### 7.1.1. Copying (копирующий алгоритм)
- Используется главным образом в **Young Generation**
- Живые объекты копируются из одной области (Eden) в другую (Survivor), тем самым устраняя фрагментацию
- Позволяет очень быстро выполнять сборку мусора за счёт линейного прохода

### 7.1.2. Mark-Sweep (пометка-очистка)
- JVM помечает достижимые объекты, затем удаляет недостижимые
- Минус: остаётся фрагментация памяти

### 7.1.3. Mark-Compact (пометка-уплотнение)
- После удаления мусора оставшиеся объекты перемещаются, чтобы устранить фрагментацию
- Используется в сборке старшего поколения

### Generational GC — **не отдельный алгоритм**, а стратегия
Generational GC (поколенческая модель) объединяет вышеперечисленные алгоритмы:
- Young Generation обычно использует **Copying**
- Old Generation — **Mark-Sweep** или **Mark-Compact**

> Generational GC — это **комбинация алгоритмов + наблюдение, что большинство объектов живут недолго**, а не самостоятельный алгоритм.

---

## 7.2. Stop-the-world (STW)
Некоторые фазы GC требуют остановки всех потоков приложения. JVM приостанавливает выполнение программы, чтобы:

- собрать корневые ссылки (GC Roots)
- некоторые из GC могут выполнять маркировку. (Хотя в современные GC выносят этот процесс параллельно с работой приложения)
- переместить или освободить объекты

Даже самые современные GC имеют короткие, но неизбежные STW-паузы.

---

## 7.3. Основные GC HotSpot

### **Serial GC**
- Однопоточный GC
- Всегда выполняет **stop-the-world**
- Подходит для небольших heap и простых задач

### **Parallel GC**
- Как и Serial GC, инициирует **полные STW-паузы**
- Использует **многопоточность** для ускорения:
    - обхода графа достижимых объектов
    - маркировки и очистки
- Основная цель — **максимальная пропускная способность (throughput)**

### **G1 GC (Garbage First)**
- Делит кучу (Heap) на множество **регионов фиксированного размера**
- Это позволяет:
    - приоритизировать сборку регионов, содержащих больше мусора (Garbage‑First)
    - избегать длительных пауз за счёт инкрементальной региональной сборки
- Стремится обеспечить **предсказуемые задержки**; является GC по умолчанию в HotSpot начиная с JDK 9

### **ZGC**
- Очень короткие паузы — как правило, в пределах нескольких миллисекунд (иногда субмиллисекундные), при этом их длительность масштабируется с размером активного набора, а не всей кучи
- Практически все операции GC выполняются параллельно с работой приложения
- Поддерживает огромные объёмы кучи (вплоть до терабайт)
- С Java 21 ZGC стал поколенческим https://openjdk.org/jeps/439

### **Shenandoah**
- Параллельно перемещает объекты (concurrent compaction), минимизируя паузы
- Создан Red Hat, работает в OpenJDK

| Характеристика              | Serial GC                            | Parallel GC                                      | G1 GC                              | ZGC                             | Shenandoah                     |
|-----------------------------|--------------------------------------|--------------------------------------------------|------------------------------------|---------------------------------|--------------------------------|
| **STW-паузы**               | Долгие, однопоточные                 | Долгие, но многопоточные                         | Короткие, прогнозируемые           | Субмиллисекундные               | Очень короткие                 |
| **Многопоточность**         | ❌ нет                                | ✔ да (маркировка и очистка)                      | ✔ да (региональная модель)         | ✔ полная параллельность         | ✔ полная параллельность        |
| **Работа с большими кучами** | Плохо                                | Удовлетворительно                                | Хорошо                             | Отлично (до терабайт)           | Очень хорошо                   |
| **Модель памяти**           | Generational                         | Generational                                     | Generational + Region-based        | Region-based + Colored Pointers | Region-based + Brooks Pointers |
| **Цель оптимизации**        | Простота, минимальная нагрузка       | Максимальная пропускная способность (throughput) | Минимизация пауз и предсказуемость | Минимизация пауз                | Минимизация пауз               |
| **Перемещение объектов**    | Да, но STW                           | Да, но STW                                       | Да, частично concurrent            | ✔ concurrent relocation         | ✔ concurrent relocation        |
| **Фрагментация памяти**     | Возможна                             | Возможна                                         | Минимизируется                     | Нет (concurrent compaction)     | Нет (concurrent compaction)    |
| **Использование CPU**       | Низкое                               | Высокое                                          | Среднее                            | Высокое                         | Высокое                        |
| **Подходит для**            | Малых приложений, однопоточных задач | High-throughput сервисов                         | Большинства серверных приложений   | Low-latency систем              | Low-latency систем             |
| **По умолчанию в JVM**      | Нет                                  | Нет                                              | ✔ Да (с JDK 9)                     | Нет                             | Нет                            |

### JVM флаги для указания желаемого GC

- -XX:+UseSerialGC
- -XX:+UseParallelGC
- -XX:+UseG1GC
- -XX:+UseZGC
- -XX:+UseShenandoahGC

Другие полезные флаги:

- -XX:+PrintCommandLineFlags -- просмотр выбранного GC
- -Xlog:gc -- включить логи GC

---

## 7.4. Типы ссылок
Поведение GC зависит от типа ссылки. 

### **Strong Reference** (сильная ссылка)
```java
Object o = new Object();
```
Объект **никогда** не удаляется, пока есть хотя бы одна сильная ссылка.

### **Soft Reference**
```java
SoftReference<Object> ref = new SoftReference<>(new Object());
Object value = ref.get();
```
GC очищает объект **только при нехватке памяти**. Часто используется для кешей.

### **Weak Reference**
```java
WeakReference<Object> ref = new WeakReference<>(new Object());
Object value = ref.get();
```
Объект удаляется при **первом же проходе GC**, если нет сильных ссылок.

Пример со **WeakHashMap**:
```java
Map<Object, String> map = new WeakHashMap<>();
Object key = new Object();
map.put(key, "value");
key = null;
System.gc();
```

Стоит отметить, что самого по себе вызова System.gc() недостаточно — элемент исчезает только при следующей структурной операции над Map.

После прохождения GC и следующей операции над картой (например, `get/put/size`), элемент будет удалён, потому что ключ хранится как weak reference. Важно: `System.gc()` — лишь рекомендация для JVM и не гарантирует немедленный сбор мусора.

### **Phantom Reference**
```java
ReferenceQueue<Object> q = new ReferenceQueue<>();
PhantomReference<Object> ref = new PhantomReference<>(new Object(), q);
```
- `ref.get()` **всегда возвращает null**.
- Ссылка используется для отслеживания момента, когда объект готов к удалению.
- Применяется для управления нативными ресурсами.

---

<details>
<summary><strong>📘 Вопросы для самопроверки</strong></summary>

1. Чем отличаются Mark-Sweep и Mark-Compact?
2. Почему stop-the-world является обязательной фазой GC?
3. В чём разница между Serial и Parallel GC?
4. Как отличается поведение SoftReference от WeakReference?

</details>

