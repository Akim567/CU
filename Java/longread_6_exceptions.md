# Конспект лекции 5 — Исключения

## 📘 Введение

Java предоставляет встроенный механизм обработки ошибок — **исключений (exceptions)**. Исключение — это один из ключевых инструментов, с помощью которого программа может корректно реагировать на нештатные ситуации, не нарушая основную логику.

### Зачем нужен механизм исключений

В предыдущих лекциях мы уже сталкивались с некоторыми ошибками времени выполнения, которые бросают стандартные исключения.

Теперь мы разберёмся, как устроен механизм исключений изнутри:
- какие типы можно бросать
- как их обрабатывать с помощью механизма `try-catch-finally`
- как создавать собственные исключения

## 1. Способы обработки ошибок в Java

В Java существует несколько способов обработки ошибок, но не все из них являются эффективными или удобными в поддержке. Давайте рассмотрим различные подходы и выделим рекомендуемый.

---

### ❌ 1.1. `null` как маркер ошибки

```java
public Order createOrder(Long productId, int count) {
    if (count <= 0) {
        return null;
    }

    Order order;
    // бизнес-логика создания заказа
    return order;
}
```

**Проблемы такого подхода:**
- Неочевидно, что означает `null`: ошибка или отсутствие результата?
- Невозможно использовать этот подход в методах `void`.
- Невозможно узнать, **почему** произошла ошибка.
- Пропущенные проверки на `null` могут привести к `NullPointerException`.

---

### ❌ 1.2. Специальный объект-результат с полем ошибки

```java
public class CreateOrderResult {
    private Order order;
    private String error;

    public CreateOrderResult(Order order, String error) {
        this.order = order;
        this.error = error;
    }

    public Order getOrder() { return order; }
    public String getError() { return error; }
}

public CreateOrderResult createOrder(Long productId, int count) {
    if (count <= 0) {
        return new CreateOrderResult(null, "Количество должно быть положительным");
    }

    Order order;
    // бизнес-логика создания заказа
    return new CreateOrderResult(order, null);
}
```

**Минусы:**
- Приходится вручную проверять, что вернулось (`order` или `error`).
- Увеличивается количество шаблонного кода.
- Становится сложно понять, где именно произошла ошибка.
- Не подходит для вложенных вызовов: если `createOrder()` вызывается из другого метода, требуется проверять `error` вручную на каждом уровне.

---

### ✔ 1.3. Go-style: результат + ошибка

> 💡 Такой подход применяется в языке Go, но **не является идиоматичным** для Java.

```go
f, err := os.Open("filename.ext")
if err != nil {
    log.Fatal(err)
}
// используем f
```

В Go ошибки — обычные значения, которые возвращаются вторым аргументом. Разработчик обязан **на каждом шаге** обрабатывать или пробрасывать их.

---

### ✅ 1.4. Java-style: исключения

Рекомендуемый подход в Java — использовать исключения для сигнализации об ошибке:

```java
public Order createOrder(Long productId, int count) {
    if (count <= 0) {
        throw new IllegalArgumentException("Количество должно быть положительным");
    }

    Order order;
    // бизнес-логика
    return order;
}
```

**Преимущества:**
- Основная логика не захламлена обработкой ошибок.
- Можно централизованно обрабатывать ошибки **выше по стеку**, а не на каждом шаге.
- Исключения могут нести с собой полезную информацию (сообщение, причину ошибки, стек вызовов).
- Можно использовать иерархию типов, создавать свои подклассы исключений.

---

### 🔁 1.5. Повторение: с какими исключениями мы уже сталкивались?

На предыдущих лекциях уже встречались примеры исключений:

- `NullPointerException` — при попытке вызова метода на `null`.
- `ArithmeticException` — при делении на 0.
- `ClassCastException` — при ошибке приведения типа.
- `StackOverflowError` — при бесконечной рекурсии.
- `OutOfMemoryError` — при нехватке памяти.

---

### 💡 Выводы

| Подход                           | Является ошибкой | Несёт информацию | Подходит для void | Удобен при вложенности |
|----------------------------------|------------------|------------------|-------------------|------------------------|
| `null`                           | ❌               | ❌               | ❌                | ❌                     |
| Объект с полем `error`           | 🔸               | 🔸               | 🔸                | ❌                     |
| Go-style: `(result, error)`      | ❌ (для Java)     | ✅               | 🔸                | ❌                     |
| Java-style: `throw new ...`      | ✅               | ✅               | ✅                | ✅                     |

> ✅ — хорошо подходит  
> 🔸 — с ограничениями  
> ❌ — не рекомендуется

<details>
  <summary>📒 Вопросы для самопроверки</summary>
  <p>1. Какие проблемы возникают при использовании `null` в качестве маркера ошибки?</p>
  <p>2. Почему подход с объектом, содержащим `error`, не подходит для вложенных вызовов?</p>
  <p>3. Какие преимущества даёт использование исключений (`throw new ...`) в Java?</p>
  <p>4. Какие типы исключений вы уже встречали в предыдущих лекциях и в чём их особенности?</p>
</details>

---

## 2. Что может быть исключением и когда его бросают

### 2.1. Только `Throwable` и его наследники

В Java выбрасывать (`throw`) можно только объекты, которые являются наследниками класса `Throwable`.

Пример допустимого типа:

```java
public class CreateOrderException extends Throwable {
}
```

Также допустимы любые наследники:

```java
public class CreateOrderException extends Exception {
}

public class CreateOrderException extends RuntimeException {
}
```

❌ Попытка выбросить объект любого другого типа (например, `String`, `Object`) вызовет ошибку компиляции.

---

### 2.2. Как выбрасываются исключения

Исключения выбрасываются с помощью ключевого слова `throw`:

```java
if (count <= 0) {
    throw new CreateOrderException();
}
```

📌 Можно бросать:
- собственные типы (например, `CreateOrderException`)
- стандартные исключения (`IllegalArgumentException`, `NullPointerException`, и т. д.)
- любые сторонние исключения (например импортированные из подключенных библиотек)

> 💡 В промышленной разработке считается хорошей практикой бросать собственные типы исключений с понятным сообщением, а стандартные исключения обрабатывать. Бросать `NullPointerException` считается плохой практикой. 

---

### 2.3. Что может **вызвать** исключение

1. **Явный `throw` в коде**
2. **Методы, которые уже бросают исключения**  
   Например, `Integer.parseInt(...)` — может бросить `NumberFormatException`.
3. **Сама виртуальная машина** (JVM) может сгенерировать исключения при ошибках исполнения:
    - `NullPointerException`
    - `ClassCastException`
    - `OutOfMemoryError`
    - `StackOverflowError`
    - `ArithmeticException`

Примеры:

```java
Object o = "hello";
Integer i = (Integer) o; // ClassCastException
```

```java
String s = null;
System.out.println(s.length()); // NullPointerException
```

```java
int[] array = new int[Integer.MAX_VALUE]; // OutOfMemoryError
```

```java
void recursive() {
    recursive(); // StackOverflowError
}
```

```java
int c = a / b; // ArithmeticException, если b == 0
```

---

### 2.4. Когда следует бросать исключение

Бросать исключения стоит, когда **метод не может продолжать выполнение** из-за нарушенного контракта, невалидных аргументов, или невозможности корректно обработать ситуацию.

Пример:

```java
public Order createOrder(Long productId, int count) {
    if (count <= 0) {
        throw new IllegalArgumentException("Количество должно быть положительным");
    }

    Order order;
    // бизнес-логика создания заказа
    return order;
}
```

🧨 Исключение сообщает вызывающему коду: "вы передали мне невалидные данные — я отказываюсь работать".

---


---

<details>
  <summary>📒 Вопросы для самопроверки</summary>
  <p>1. Какие типы можно выбрасывать в Java с помощью `throw`?</p>
  <p>2. Что произойдёт, если попытаться выбросить объект, не являющийся наследником `Throwable`?</p>
  <p>3. Какие три источника могут приводить к возникновению исключений?</p>
  <p>4. Приведи примеры исключений, которые выбрасываются JVM.</p>
  <p>5. Когда корректно бросать исключения вручную?</p>
</details>

---

## 3. Создание и проектирование собственных исключений

### 3.1. Наследование от `RuntimeException` или `Exception`

Чтобы создать собственное исключение в Java, нужно наследоваться **не напрямую от `Throwable`**, а от одного из его наследников — как правило, от `RuntimeException`.

Пример:

```java
public class CreateOrderException extends RuntimeException {
}
```

> 💡 Не рекомендуется наследоваться от `Throwable` напрямую — это низкоуровневый абстрактный тип.

---

### 3.2. Поддержка конструкторов

Когда вы создаёте своё исключение, желательно переопределить **несколько конструкторов**, чтобы дать возможность:
- передать сообщение об ошибке
- вложить другую причину (`cause`). В качестве cause обычно используют исходное исключение в качестве причины ошибки.
- указать и то, и другое.

---

#### Конструктор с сообщением

```java
public class CreateOrderException extends RuntimeException {
    public CreateOrderException(String message) {
        super(message);
    }
}
```

---

#### Конструктор с причиной (`cause`)

```java
public class CreateOrderException extends RuntimeException {
    public CreateOrderException(Throwable cause) {
        super(cause);
    }
}
```

---

#### Конструктор с сообщением и причиной

```java
public class CreateOrderException extends RuntimeException {
    public CreateOrderException(String message, Throwable cause) {
        super(message, cause);
    }
}
```

> 💡 Использование `cause` позволяет сохранять **первопричину исключения**, не теряя стек вызовов и контекст. Это помогает вести отладку: вы можете проследить цепочку исключений до самого источника.

---

<details>
  <summary>📒 Вопросы для самопроверки — 3. Создание собственных исключений</summary>
  <p>1. Почему не рекомендуется наследоваться напрямую от `Throwable`?</p>
  <p>2. Зачем создавать собственные типы исключений?</p>
  <p>3. Какие типы конструкторов желательно реализовать в собственном исключении?</p>
  <p>4. Что такое причина (`cause`) и как он помогает при отладке?</p>
  <p>5. Как выглядит пример обёртывания одного исключения в другое?</p>
</details>

## 4. Обработка исключений: try-catch-finally

### 4.1. Структура блока try-catch

Чтобы отловить и обработать исключение, используется конструкция:

```java
try {
    // опасный код
} catch (IllegalArgumentException e) {
    // обработка ошибки
}
```
💡 В блок `try` помещается код, который **может выбросить исключение**.  
Если во время выполнения возникает исключение совместимого типа, управление передаётся в `catch`.

---

### 4.2. Что ловит catch

`catch` блок обрабатывает **указанный тип исключения и всех его наследников**:

```java
try {
    // опасный код
} catch (IllegalArgumentException e) {
    // обработка IllegalArgumentException и его наследников
}
```

---

### 4.3. Порядок блоков catch

Порядок блоков catch имеет значение. Catch родительских классов всегда должны располагаться ниже наследников

```java
try {
    // опасный код
} catch (RuntimeException e) {
    // обработка RuntimeException и всех его наследников
} catch (IllegalArgumentException e) {
    // ❌ недостижимый блок
}
```

> ⚠️ Компилятор не позволит такой код, потому что `IllegalArgumentException` — это наследник `RuntimeException`, и он уже будет перехвачен выше.

---

### 4.4. Multi-catch

Если код обработки одинаковый для нескольких исключений, можно использовать **multi-catch**:

```java
try {
    // опасный код
} catch (IllegalStateException | IllegalArgumentException e) {
    // обработка ошибок
} catch (NullPointerException | ArithmeticException e) {
    // другая обработка ошибок
}
```

---

### 4.5. Область видимости try-catch

Переменные, объявленные внутри блока `try`, **недоступны в `catch` и вне `try-catch`**:

```java
public void purchase(Long productId, int count) {
    try {
        Order order = orderService.createOrder(productId, count);
    } catch (CreateOrderException e) {
        Long id = order.getId(); // ❌ ошибка компиляции — order не виден
    }
}
```

Чтобы использовать переменные вне `try`, объявляйте их заранее:

```java
Order order;
try {
    order = orderService.createOrder(productId, count);
} catch (CreateOrderException e) {
    // обработка
}
Long id = order.getId(); // ✅ переменная видна
```

---

### 4.6. Методы класса Throwable

При обработке исключений часто используются методы:

- `e.getMessage()` — текст сообщения
- `e.getCause()` — причина исключения (если была вложена)
- `e.printStackTrace()` — стек вызовов

---

### 4.7. Блок finally

`finally` — блок, который **всегда выполняется**, даже если возникло исключение:

```java
try {
    // опасный код
} catch (Exception e) {
    // обработка
} finally {
    // выполняется в любом случае
}
```

Он используется для **освобождения ресурсов**, например, закрытия файлов, соединений, отката транзакций. 
Пример случая, когда с помощью finally можно восстановить первоначальное состояние объекта: заказ формируется, в блоке try происходит его обработка, в catch обрабатывается ошибка и выставляется терминальный статус, а в finally можно вернуть товар на склад.

---

#### Поведение при пробросе исключения

Если исключение не было перехвачено, `finally` всё равно выполнится.  
❗ После выполнения `finally` — исключение продолжит проброс **вверх по стеку вызовов**.

```java
try {
    dangerous(); // может выбросить исключение
} finally {
    cleanup();   // выполнится в любом случае
}
// ❗ если исключение не было поймано — улетит выше по стеку, то есть по цепочке вызывающих методов
```

---

#### 🚫 Не выбрасывать исключения из finally

```java
try {
    // опасный код
} catch (Exception e) {
    throw new SomeException();
} finally {
    throw new SomeOtherException(); // ❌ перетрёт предыдущее исключение
}
```

> ⚠️ Исключение из `finally` перезапишет то, что было выброшено ранее — исходная ошибка будет потеряна.

---

#### Пример: освобождение ресурса

```java
public void cancelOrder(Long orderId) {
    try {
        orderService.refund(orderId);
    } catch (PaymentServiceException e) {
        // обработка ошибки возврата средств
    } finally {
        stockService.unfreezeStocks(orderId);
    }
}
```

Здесь товар **размораживается всегда**, независимо от успешности возврата средств. Это позволяет не блокировать склад.

### 🚫 Не используем исключения для управлением логикой программы

Плохая практика:

```java
try {
    int c = a / b;
} catch (ArithmeticException e) {
    c = 0; // 🤢 это лучше делать через if
}
```

Хорошая практика:

```java
if (b != 0) {
    int c = a / b;
} else {
    int c = 0;
}
```

> 💡 Исключения — это не замена `if`. Это сигнал о **нарушении контракта**, а не способ построения логики программы.


---

<details>
  <summary>📒 Вопросы для самопроверки</summary>
  <p>1. Что происходит, когда возникает исключение в блоке `try`?</p>
  <p>2. Почему порядок блоков `catch` имеет значение?</p>
  <p>3. Что такое multi-catch и когда он полезен?</p>
  <p>4. Почему нельзя использовать переменные из `try` в `catch`, если они объявлены внутри?</p>
  <p>5. Когда выполняется `finally` и что происходит, если исключение не было перехвачено?</p>
  <p>6. Почему опасно выбрасывать исключения из `finally`?</p>
  <p>7. Почему не следует использовать `try-catch` как замену условным операторам?</p>
</details>

## 5. Хорошие и плохие практики обработки исключений

---

#### 🚫 Игнорирование исключения

```java
try {
    order = orderService.createOrder(productId, count);
} catch (IllegalArgumentException e) {
}
```

- Исключение подавляется, программа продолжает выполнение как будто ничего не произошло.
- Это может привести к работе с **некорректным или пустым объектом**, неожиданным побочным эффектам.
- Диагностировать такие ошибки сложно.

---

#### 🚫 Повторный проброс без обработки

```java
try {
    order = orderService.createOrder(productId, count);
} catch (IllegalArgumentException e) {
    throw e;
}
```

- Нет смысла в `try-catch`, поведение будет тем же, что и без них.
- Может ввести в заблуждение — кажется, что исключение обработано.

---

#### 🚫 Логирование и проброс

```java
try {
    order = orderService.createOrder(productId, count);
} catch (IllegalArgumentException e) {
    System.err.println("Ошибка при создании заказа");
    throw e;
}
```

- Часто приводит к **дублированию логов**: один раз здесь, второй — на более высоком уровне.
- Лучше логировать там, где принимается решение о последствиях ошибки.

---

#### ✅ Продолжать выполнение, если это безопасно

```java
for (var order : orders) {
    try {
        notifyService.notify(order.getUserId(), "Заказ " + order.getOrderId() + " доставлен");
        order.setNotificationSent(true);
    } catch (SmsSendingException e) {
        System.err.println("Ошибка для " + order.getUserId());
        order.setNotificationSent(false);
    }
}
```

- Можно использовать, если возникшее исключение не должно прерывать всю логику – то есть продолжение работы метода возможно.
- Таким образом, поведение системы будет устойчиво к частичным сбоям.
- Пользователь получает корректную информацию даже при ошибках.
---

#### ✅ Оборачивать исключения в исключения домена

```java
try {
    order = orderService.createOrder(productId, count);
} catch (IllegalArgumentException e) {
    throw new OrderValidationFailed("Неверные данные", e);
}
```

- Сохраняется **исходная причина** ошибки (`cause`);
- Позволяет:
    - скрыть детали реализации:
        - с какими сервисами и библиотеками взаимодействует система,
        - технические детали низкоуровневой ошибки;
    - вернуть **понятное исключение**, связанное с предметной областью.

---

#### 🚫 Ловить `Throwable`

```java
try {
    order = orderService.createOrder(productId, count);
} catch (Throwable e) {
    System.err.println("Ошибка");
    throw e;
}
```

- Ловятся **все ошибки JVM**, включая `OutOfMemoryError`, `StackOverflowError`, `AssertionError`.
- Такие ошибки не предполагают восстановления.
- Это **антипаттерн**, ловить стоит только `Exception` (или лучше — его конкретные подтипы).

---

#### 🚫 Ловить `Exception`

```java
try {
    order = orderService.createOrder(productId, count);
} catch (Exception e) {
    System.err.println("Ошибка");
    throw e;
}
```

- Перехватываются все исключения, включая те, которые **не связаны с текущим контекстом**.
- Трудно понять, что именно пошло не так, так как тип перехватываемо исключения слишком широкий.
- Нарушает принцип *fail fast*.

---

#### ✅ Обрабатывать максимально специфичное исключение

```java
try {
    order = orderService.createOrder(productId, count);
} catch (OrderValidationFailed e) {
    System.err.println("Ошибка валидации данных заказа");
}
```

- Повышает **читаемость** и **надежность**.
- Позволяет явно разделить типы ошибок и корректно на них реагировать.

---

#### ✅ Ловить исключение, относящееся к конкретной библиотеке

```java
OrderDto order;

try {
    order = orderService.parse(jsonOrder);
} catch (JsonProcessingException e) {
    System.err.print("Не удалось спарсить json");
}
```

- В данном случае будут пойманы все ошибки библиотеки

#### 🚫 Слишком большой блок `try`

```java
try {
    List<Long> ids = deliveryService.getDelivered();
    List<Order> orders = orderService.getOrdersByIds(ids);
    for (var order : orders) {
        String msg = "Заказ " + order.getOrderId() + " доставлен";
        notifyService.notify(order.getUserId(), msg);
    }
} catch (RuntimeException e) {
    System.err.println("Ошибка при уведомлении");
}
```

- Невозможно точно понять, какая строка вызвала исключение.
- Обработка ошибки не соответствует месту её возникновения.
- Сложно писать корректные юнит-тесты и логировать с нужным контекстом.

---

#### ✅ Минимизировать блок `try`

```java
for (var order : orders) {
    try {
        notifyService.notify(order.getUserId(), "Заказ " + order.getOrderId() + " доставлен");
    } catch (SmsSendingException e) {
        System.err.println("Ошибка отправки для " + order.getUserId());
    }
}
```

- Чётко видно, **где возникла ошибка**.
- Упрощает отладку и обработку по месту.

---

| Плохой подход 🚫                                              | Хороший подход ✅                                         |
|---------------------------------------------------------------|----------------------------------------------------------|
| Игнорировать исключение                                       | Логировать исключение и продолжать работу, если возможно |
| Ловить и сразу пробрасывать исключение дальше                 | Обернуть исключение в кастомное и пробросить дальше      |
| Логировать исключение и пробрасывать пробросить дальше его же | Обернуть исключение в кастомное и пробросить дальше      |
| Ловить Throwable, Exception                                   | Ловить специфичное исключение                            |
| Помещать много логики в try                                   | В try писать минимальное количество логики               |


<details>
  <summary>📒 Вопросы для самопроверки</summary>
  <p>1. Почему пустой `catch` блок считается плохой практикой?</p>
  <p>2. Чем плох повторный проброс без логики обработки?</p>
  <p>3. В чём проблема логирования и проброса одного и того же исключения?</p>
  <p>4. Почему не стоит ловить `Throwable`?</p>
  <p>5. Что означает "обрабатывать максимально специфичное исключение"?</p>
  <p>6. Зачем оборачивать исключения в исключения предметной области?</p>
  <p>7. Почему важно минимизировать блок `try`?</p>
  <p>8. Когда можно безопасно продолжать выполнение после исключения?</p>
</details>

## 6. Раскрутка стека (Stack Unwinding)

---

### Что такое stack unwinding?

Если исключение не перехвачено в текущем методе, оно автоматически передаётся вверх по **стеку вызовов** — этот процесс называется **stack unwinding** (раскрутка стека).

Исключение будет пробрасываться до тех пор, пока:
- не встретится подходящий `catch`-блок;
- или стек не будет полностью раскручен (программа завершится с ошибкой).

---

### Пример: цепочка вызовов

```java
public void notify(Long userId, String message) {
    var phone = personalDataService.getPhone(userId);
    smsSender.send(phone, message);
}

public void send(String phone, String message) {
    try {
        sendSms(phone, message);
    } catch (NetworkException e) {
        throw new SmsSendingException(e);
    }
}
```

- Метод `sendSms` может выбросить `NetworkException`.
- Этот `NetworkException` оборачивается в `SmsSendingException`.
- Но метод `send` **не обрабатывает** `SmsSendingException` — стек начинает раскручиваться дальше вверх, к `notify`.

---

### Где перехватываем исключение

```java
try {
    notifyService.notify(order.getUserId(), msg);
} catch (SmsSendingException e) {
    System.err.println(
        "Не смогли оповестить пользователя " +
        order.getUserId()
    );
}
```

- Здесь `SmsSendingException` успешно перехватывается.
- Это **пример корректного использования** stack unwinding:
    - ошибки низкоуровневого взаимодействия (SMS) обрабатываются **выше**, где есть **контекст пользователя**.

---

### Выводы

- Исключения «всплывают» вверх по стеку вызовов до тех пор, пока не будут перехвачены.
- Это позволяет:
    - сосредоточить обработку ошибок там, где есть необходимый **контекст**;
    - избегать лишнего `try-catch` на каждом уровне вызова.

---

<details>
  <summary>📒 Вопросы для самопроверки</summary>
  <p>1. Что такое stack unwinding?</p>
  <p>2. Что произойдёт, если исключение не перехвачено в текущем методе?</p>
  <p>3. Почему в нашем примере `SmsSendingException` оборачивается, а не пробрасывается напрямую?</p>
  <p>4. Почему полезно ловить исключения на уровне, где есть контекст?</p>
</details>

---
## 7. Иерархия исключений

Все исключения в Java — это наследники класса `Throwable`.

![exceptions.png](assets/exceptions.png)

---

### Классификация

- Проверяемые (**Checked**): все наследники `Exception`, кроме `RuntimeException` и её наследников
- Непроверяемые (**Unchecked**): `RuntimeException`, `Error` и все их наследники

---

### Checked-исключения: обязаны быть **либо пойманы**, либо **объявлены**

```java
public class SmsSendingException extends Exception {}

public class SmsSender {
    public void send(String phone, String message) throws SmsSendingException {
        ...
    }
}
```

Если вы используете метод, который выбрасывает `SmsSendingException`, компилятор потребует:

#### Вариант 1: указать в сигнатуре

```java
public void notify(Long userId, String message) throws SmsSendingException {
    String phone = personalDataService.getPhone(userId);
    smsSender.send(phone, message);
}
```

#### Вариант 2: обернуть в try-catch

```java
public void notify(Long userId, String message) {
    String phone = personalDataService.getPhone(userId);
    try {
        smsSender.send(phone, message);
    } catch (SmsSendingException e) {
        // обработка
    }
}
```

---

### Ошибка компиляции

Если не указать `throws` и не обернуть в `try-catch`, будет ошибка компиляции:

```java
public void notify(Long userId, String message) {
    String phone = personalDataService.getPhone(userId);
    smsSender.send(phone, message); // ❌ Compile-time error
}
```

---

### Unchecked-исключения: можно **не ловить** и **не указывать**

```java
public class OrderValidationException extends RuntimeException {}

public class Validator {
    public void validate(Order order)
            throws OrderValidationException {} // можно указать, можно не указывать
}
```

Можно и вовсе не писать `throws`, компилятор не возразит:

```java
public class Validator {
    public void validate(Order order) {
        // валидация
    }
}
```

---

### Исключения в сигнатуре метода

Метод может **объявить**, что он выбрасывает исключения, — с помощью ключевого слова `throws` в сигнатуре. Это становится частью **контракта метода**.

```java
public Order createOrder(Long productId, int count) throws OrderValidationException, NetworkException {
    // ...
}
```

Что это значит:

- **Вызывающая сторона обязана** либо:
    - обернуть вызов метода в `try-catch`,
    - либо также указать `throws` в своей сигнатуре.
- Компилятор проверяет выполнение этого контракта (если исключение — `checked`).

Такой контракт позволяет:
- **предупредить вызывающий код** о возможных сбоях;
- делегировать обработку ошибок вверх по стеку.

---

### 💡 Выводы

|                            | Checked Exception                  | Unchecked Exception                      |
|----------------------------|------------------------------------|------------------------------------------|
| Наследник                 | `Exception` (кроме `RuntimeException`) | `RuntimeException`, `Error` и их наследники |
| Обязаны обрабатывать?     | ✅ Да                              | ❌ Нет                                   |
| Указывать в throws?       | ✅ Да                              | ❌ Нет                                   |
| Контролируется компилятором? | ✅ Да                              | ❌ Нет                                   |

---

<details>
  <summary>📒 Вопросы для самопроверки</summary>
  <p>1. Что означает `throws` в сигнатуре метода?</p>
  <p>2. Чем отличаются checked и unchecked исключения?</p>
  <p>3. Какие исключения компилятор требует обязательно обработать?</p>
  <p>4. Можно ли поймать unchecked исключение, даже если метод его не выбрасывает?</p>
  <p>5. Что произойдёт, если не указать `throws` для checked исключения?</p>
</details>

## 8. Исключения и сигнатура метода при наследовании

---

### Поведение при переопределении метода

Когда вы переопределяете метод в подклассе, вы обязаны **соблюдать контракт родителя**, включая объявление `throws` для проверяемых (checked) исключений.

---

#### ✅ 1. Оставить тот же список исключений

```java
public abstract class OrderService {
    public abstract Order createOrder(Long productId, int count) throws IllegalArgumentException;
}

public class OnlineOrderService extends OrderService {
    @Override
    public Order createOrder(Long productId, int count) throws IllegalArgumentException {
        // ...
    }
}
```

---

#### ✅ 2. Уточнить (сузить) тип исключения — заменить на наследника

```java
public abstract class OrderService {
    public abstract Order createOrder(Long productId, int count) throws IllegalArgumentException;
}

public class OnlineOrderService extends OrderService {
    @Override
    public Order createOrder(Long productId, int count) throws InvalidParameterException {
        // ...
    }
}
```

> Это возможно, потому что `InvalidParameterException` — наследник `IllegalArgumentException`.

---

#### ✅ 3. Убрать `throws` полностью

```java
public abstract class OrderService {
    public abstract Order createOrder(Long productId, int count) throws IllegalArgumentException;
}

public class OnlineOrderService extends OrderService {
    @Override
    public Order createOrder(Long productId, int count) {
        // ...
    }
}
```

> Метод всё ещё совместим: он **гарантирует**, что не бросит исключения, которые ожидались в базовом контракте.

---

#### ❌ 4. Расширить список — добавить новый checked-тип

Это **нельзя**. Пример ниже вызовет ошибку компиляции:

```java
public abstract class OrderService {
    public abstract Order createOrder(Long productId, int count) throws IllegalArgumentException;
}

public class OnlineOrderService extends OrderService {
    @Override
    public Order createOrder(Long productId, int count) throws Throwable { // ❌ ошибка
        // ...
    }
}
```

> Нельзя добавить новый checked-тип, которого не было в базовой сигнатуре — это нарушит контракт.

---

### Почему это важно?

- Клиентский код работает с **типом родителя**.
- Если родитель гарантирует, что метод может выбросить только `IllegalArgumentException`, вызывающий код **не обязан** ловить другие исключения.
- Расширение списка нарушает контракт и может привести к ошибкам компиляции.

---

<details>
  <summary>📒 Вопросы для самопроверки</summary>
  <p>1. Почему при переопределении нельзя расширить список исключений?</p>
  <p>2. Когда допустимо заменить исключение на его наследника?</p>
  <p>3. Можно ли полностью убрать `throws` в переопределённом методе?</p>
  <p>4. Почему важно соблюдать сигнатуру метода при переопределении?</p>
</details>