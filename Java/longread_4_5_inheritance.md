# Конспект лекции 4 — Наследование

## Введение

В предыдущих лекциях мы разобрали базовые основы Java:

- **Лекция 1** — познакомились с языком, его особенностями и синтаксисом.
- **Лекция 2** — разобрали объекты и классы: что такое поля, методы, конструкторы, копирование объектов.
- **Лекция 3** — изучили расширенные возможности классов: модификатор `final`, неизменяемые классы, `records`, статические поля и методы, пакеты, вложенные и внутренние классы.

Теперь мы переходим к **наследованию (inheritance)** — ключевому механизму объектно-ориентированного программирования.

### Зачем нужно наследование?

1. **Устранение дублирования кода**  
   Когда несколько классов имеют общие поля и методы, их можно вынести в базовый класс.

2. **Единый источник правды**  
   Общая логика поддерживается в одном месте. Изменение в базовом классе автоматически применяется во всех наследниках.

3. **Переопределение поведения**  
   Класс-наследник может адаптировать или расширять поведение родительского класса.

4. **Иерархия типов**  
   Позволяет описывать связи «is-a» (дебетовая карта — это карта, кредитная карта — это карта).

### Пример из жизни

У нас есть несколько видов банковских карт:

- **DebitCard** — дебетовая карта, работает с балансом.
- **CreditCard** — кредитная карта, работает с лимитом и задолженностью.
- **JuniorDebitCard** — специальная разновидность дебетовой карты.

У этих классов много общего:
- поля: номер карты, валюта, срок действия;
- логика: проверка истечения срока действия.

Если реализовать каждый класс отдельно, придётся дублировать код, что ведёт к ошибкам и усложняет поддержку.  
**Решение — наследование:** общие элементы выносятся в базовый класс `Card`, а наследники переопределяют и дополняют поведение.

У кредитной и дебетовой карты много общего: состояние и логика

```java
public class DebitCard {
    private String cardNumber;
    private Currency currency;
    private BigDecimal balance;
    private OffsetDateTime expiryDateTime;

    public void makePayment(BigDecimal amount) {
        if (balance.compareTo(amount) >= 0) {
            balance = balance.subtract(amount);
        }
    }

    public boolean isExpired(OffsetDateTime now) {
        return now.isAfter(this.expiryDateTime);
    }
}

public class CreditCard {
    private String cardNumber;
    private Currency currency;
    private BigDecimal creditLimit;
    private BigDecimal currentDebt;
    private OffsetDateTime expiryDateTime;

    public void makePayment(BigDecimal amount) {
        BigDecimal availableBalance = creditLimit.subtract(currentDebt);
        if (availableBalance.compareTo(amount) >= 0) {
            currentDebt = currentDebt.add(amount);
        }
    }

    public boolean isExpired(OffsetDateTime now) {
        return now.isAfter(this.expiryDateTime);
    }
}
```

Каждый раз при добавлении нового типа карты мы вынуждены писать один и тот же код:

```java
public class JuniorDebitCard {
    private String cardNumber;
    private Currency currency;
    private BigDecimal balance;
    private OffsetDateTime expiryDateTime;

    public void makePayment(BigDecimal amount) {
        if (balance.compareTo(amount) >= 0) {
            balance = balance.subtract(amount);
        }
    }

    public boolean isExpired(OffsetDateTime now) {
        return now.isAfter(this.expiryDateTime);
    }
}
```

Здесь мы видим три проблемы:

1. **Дублирование кода**  
   Методы `isExpired(...)` и поля (`cardNumber`, `currency`, `expiryDateTime`) повторяются в каждом классе.

2. **Усложнение поддержки**  
   Если изменится логика проверки срока действия карты, придётся править код во всех классах.

3. **Отсутствие единого источника правды**  
   Из-за копирования кода логика может «разъехаться» — один класс будет работать по одним правилам, другой по другим.

➡️ **Вывод:** наследование решает эти проблемы.  
Общие поля и методы нужно вынести в базовый класс `Card`, а специализированные (`balance` у дебетовой карты, `creditLimit` у кредитной карты) оставить в наследниках.

## 1. Наследование (Inheritance)

### 1.1. Базовый класс Card

Начнём с базового класса `Card`. Он хранит общие для всех карт поля: номер, валюту, срок действия.  
Также в нём реализован метод `isExpired(...)`, который проверяет, не истёк ли срок действия карты.

```java
public class Card {
    private String cardNumber;
    private Currency currency;
    private OffsetDateTime expiryDateTime;

    public boolean isExpired(OffsetDateTime now) {
        return now.isAfter(this.expiryDateTime);
    }
}
```

💡 Замечания:
- `Card` описывает **общее состояние** (fields), свойственное всем картам.
- Метод `isExpired(...)` — пример **общей логики**, одинаковой для всех типов карт.
- Мы избегаем дублирования кода: проверку срока действия не нужно писать заново в каждом наследнике.

---

### 1.2. Класс-наследник CreditCard

Теперь создаём конкретный класс `CreditCard`, который **расширяет** `Card` через ключевое слово `extends`.  
Он добавляет собственное состояние (`creditLimit`, `currentDebt`) и поведение (`makePayment(...)`).

```java
public class CreditCard extends Card {
    private BigDecimal creditLimit;
    private BigDecimal currentDebt;

    public void makePayment(BigDecimal amount) {
        BigDecimal availableBalance = creditLimit.subtract(currentDebt);
        
        if (availableBalance.compareTo(amount) >= 0) {
            currentDebt = currentDebt.add(amount);
        }
    }
}
```

💡 Замечания:
- `CreditCard` наследует все поля и методы `Card` (включая `isExpired(...)`).
- Добавлены новые поля, специфичные для кредитных карт: `creditLimit`, `currentDebt`.
- Метод `makePayment(...)` реализует **уникальное поведение** кредитной карты.
- Таким образом, класс-наследник может как использовать общую функциональность родителя, так и добавлять свою.

---

### 1.3. Использование наследника

Посмотрим, как работает наследование в действии:

```java
void main() {
    CreditCard creditCard = new CreditCard();
    creditCard.isExpired(OffsetDateTime.now());
}
```

💡 Замечания:
- Мы создали объект `CreditCard`, но вызвали метод `isExpired(...)`, определённый в базовом классе `Card`.
- Это наглядно показывает: **классу-наследнику доступны методы родителя**.
- Такой подход уменьшает дублирование: `isExpired(...)` написан один раз, но используется всеми потомками.

---

### 1.4. Практические выводы

1. **Ключевое слово `extends`** связывает два класса: наследник получает все неприватные поля и методы родителя.
2. **Базовый класс хранит общее состояние и логику**: номер карты, валюту, проверку срока действия.
3. **Наследник добавляет уникальное поведение**: у кредитной карты это кредитный лимит и долг.
4. **Код проще поддерживать**: общие изменения делаются в одном месте (`Card`).  

<details>
  <summary>📒 Вопросы для самопроверки</summary>
  <p>1. Чем наследование отличается от простого копирования одинаковых методов в несколько классов?</p>
  <p>2. Какие риски появляются, если базовый класс перегружать лишней логикой, не относящейся ко всем наследникам?</p>
  <p>3. Как наследование влияет на сопровождаемость кода при изменении общей логики?</p>
</details>

---

## 2. Переопределение методов (@Override)

### 2.1. Зачем нужно переопределение

Иногда базовый класс задаёт **общее поведение по умолчанию**, но в наследнике нам нужно его изменить.  
В этом случае мы используем механизм **переопределения метода** (method overriding).

Ключевые правила:
- **Сигнатура метода** (имя + типы аргументов) должна совпадать.
- **Возвращаемый тип** менять нельзя (исключение — *covariant return types*, о нем подробно в пункте 8).
- **Аннотация `@Override`** помогает компилятору отлавливать ошибки.

---

### 2.2. Пример: базовый класс и наследник

В базовом классе `Card` определён метод для расчёта комиссии:

```java
public class Card {
    private String cardNumber;
    private Currency currency;
    private OffsetDateTime expiryDateTime;

    public int calculateFeePercent(BigDecimal sum) {
        return 0;
    }
}
```

В `CreditCard` мы переопределяем его под свою логику:

```java
public class CreditCard extends Card {
    private BigDecimal creditLimit;
    private BigDecimal currentDebt;
    private BigDecimal currentTransferredAmount;
    private BigDecimal nonFeeTransferLimit;

    @Override
    public int calculateFeePercent(BigDecimal sum) {
        if (currentTransferredAmount.compareTo(nonFeeTransferLimit) >= 0) {
            return 2;
        }
        return 0;
    }
}
```

💡 Замечания:
- Вызов `calculateFeePercent(...)` на объекте `Card` всегда вернёт `0`.
- На `CreditCard` — уже может вернуть `2`, если превышен лимит.
- Аннотация `@Override` подчёркивает, что метод переопределён.

---

### 2.3. Особенности переопределения метода

#### ❌ Изменение возвращаемого типа недопустимо

```java
public class CreditCard extends Card {
    private BigDecimal creditLimit;
    private BigDecimal currentDebt;
    private BigDecimal currentTransferredAmount;
    private BigDecimal nonFeeTransferLimit;

    @Override
    public float calculateFeePercent(BigDecimal sum) {
        if (currentTransferredAmount.compareTo(nonFeeTransferLimit) >= 0) {
            return 2;
        }
        return 0;
    }
}
```

Ошибка компиляции: возвращаемый тип не совпадает (`int` в родителе → `float` в наследнике).

---

#### ❌ Изменение типа, порядка и количества аргументов недопустимо

```java
public class CreditCard extends Card {
    private BigDecimal creditLimit;
    private BigDecimal currentDebt;
    private BigDecimal currentTransferredAmount;
    private BigDecimal nonFeeTransferLimit;

    @Override
    public float calculateFeePercent(String sum) {
        if (currentTransferredAmount.compareTo(nonFeeTransferLimit) >= 0) {
            return 2;
        }
        return 0;
    }
}
```

Ошибка компиляции: изменился тип аргумента (`BigDecimal` в родителе → `String` в наследнике).

---

#### ✅Переименование аргументов допустимо

```java
public class CreditCard extends Card {
    private BigDecimal creditLimit;
    private BigDecimal currentDebt;
    private BigDecimal currentTransferredAmount;
    private BigDecimal nonFeeTransferLimit;

    @Override
    public float calculateFeePercent(String x) {
        if (currentTransferredAmount.compareTo(nonFeeTransferLimit) >= 0) {
            return 2;
        }
        return 0;
    }
}
```

✅ Допустимо: имена аргументов **не входят в сигнатуру метода**.

---

#### ❌Переопределение несуществующего метода недопустимо

```java
public class CreditCard extends Card {
    private BigDecimal creditLimit;
    private BigDecimal currentDebt;
    private BigDecimal currentTransferredAmount;
    private BigDecimal nonFeeTransferLimit;

    @Override
    public float calculateFeePercent() {
        if (currentTransferredAmount.compareTo(nonFeeTransferLimit) >= 0) {
            return 2;
        }
        return 0;
    }
}
```

Ошибка компиляции: метода с такой сигнатурой нет в базовом классе. Если убрать аннотацию **@Override** в этом примере, то ошибки не будет, но и не будет переопределения метода, вместо этого будет перегрузка метода в наследнике.

---

### 2.4. Зачем нужна аннотация @Override

Аннотацию **@Override** можно не указывать, это не приведет к ошибке компиляции. Тем не менее ее **рекомендуется всегда использовать** при переопределении:

- @Override служит документацией: явно показывает, что метод из базового класса изменён.
- @Override Помогает избежать ошибок: если метод отмеченный аннотацией не совпадает по сигнатуре ни с одним из методов родителя — будет ошибка компиляции.

---

### 2.5. Использование super

Иногда нужно не заменить полностью логику метода, а **дополнить её**.  
В этом случае можно вызвать реализацию родителя через `super.methodName(...)`.

```java
public class CreditCard extends Card {
    private BigDecimal creditLimit;
    private BigDecimal currentDebt;
    private BigDecimal currentTransferredAmount;
    private BigDecimal nonFeeTransferLimit;
    
    @Override
    public int calculateFeePercent(BigDecimal amount) {
        if (currentTransferredAmount.compareTo(nonFeeTransferLimit) >= 0) {
            return 2;
        }
        return super.calculateFeePercent(amount); // можно вызвать любой неприватный метод из базового класса при наличии
    }
}
```

---

### 2.6. Практические выводы

1. Переопределение позволяет адаптировать поведение базовых методов.
2. Сигнатура метода должна совпадать: имя + типы аргументов.
3. Возвращаемый тип менять нельзя (кроме *covariant return types*, о них подробно в пункте 8).
4. Аннотация `@Override` очень желательна на практике: повышает читаемость и защищает от ошибок.
5. Через `super` можно использовать базовую реализацию и добавлять к ней расширения.  

<details>
  <summary>📒 Вопросы для самопроверки</summary>
  <p>1. Что такое переопределение метода и чем оно отличается от перегрузки?</p>
  <p>2. Почему изменение только имен аргументов не вызывает ошибку компиляции при переопределении?</p>
  <p>3. Почему аннотация @Override считается обязательной практикой в промышленном Java-коде?</p>
  <p>4. Когда может быть уместен вызов реализации родителя через super.methodName(...)?</p>
</details>

---

## 3. Работа с экземплярами и полиморфизм

### 3.1. Создание объектов базового и наследуемого класса

```java
Card card = new Card();
CreditCard creditCard = new CreditCard();
CreditCard baseAsCreditCard = new Card(); // Compilation Error
```

Здесь мы создаём экземпляры базового класса `Card` и класса-наследника `CreditCard`.  
У каждого — свои поля и методы, но у наследника доступны и методы родителя.

---

### 3.2. Присваивание наследника базовому типу

```java
Card creditAsBaseCard = new CreditCard();
```

Это называется **приведение типа вверх по иерархии классов** (**upcasting**).  
Мы можем хранить объект `CreditCard` в переменной типа `Card`, потому что `CreditCard` **is-a** `Card`.  
Это основа полиморфизма: работать с разными подтипами через единый интерфейс (базовый класс).

---

### 3.3. Динамический полиморфизм

```java
Card card = new Card();
CreditCard creditCard = new CreditCard();
Card creditAsBaseCard = new CreditCard();

int a = card.calculateFeePercent(x); // вызов Card#calculateFeePercent
int b = creditCard.calculateFeePercent(x); // вызов CreditCard#calculateFeePercent
int c = creditAsBaseCard.calculateFeePercent(x); // вызов CreditCard#calculateFeePercent
```

💡Важно: при вызове метода выбирается реализация **по фактическому типу объекта**, а не по типу переменной.
- Если объект на самом деле типа `Card`, вызовется метод из `Card`.
- Если объект на самом деле типа `CreditCard`, даже через переменную `Card` вызовется метод из `CreditCard`.
- Выбор перегруженного метода (в данном случае он один) происходит во время компиляции, а выбор переопределенной версии - во время исполнения.

---

### 3.4. Практические выводы

1. Переменной базового типа (`Card`) можно присвоить объект наследника (`CreditCard`).
2. Это позволяет писать код, работающий с базовым интерфейсом, но фактически поддерживающий множество реализаций.
3. JVM во время выполнения сама определяет, какой метод вызвать (механизм **dynamic dispatch**).

<details>
  <summary>📒 Вопросы для самопроверки — 3. Работа с экземплярами и полиморфизм</summary>
  <p>1. Можно ли присвоить объект CreditCard переменной типа Card? Почему?</p>
  <p>2. Что такое upcasting и зачем он нужен?</p>
  <p>3. Как JVM определяет, какой метод вызвать при обращении к calculateFeePercent(...)?</p>
  <p>4. Что произойдет, если вызвать метод у переменной типа Card, в которой хранится объект CreditCard?</p>
</details>

---

## 4. Приведение типов (Casting) и оператор `instanceof`

### 4.1. Присваивание и приведение
Вспомним: объект наследника можно хранить в переменной базового типа, выполняем *приведение типа вверх по иерархии* (**upcasting**).  
Но если нужно снова обратиться к специфичным методам наследника, придётся выполнить *приведение вниз по иерархии* (**downcasting**).

```java
Card base = new CreditCard();          // upcasting: OK
CreditCard credit = (CreditCard) base; // downcasting: OK
```

Здесь:
- `base` хранит объект типа `CreditCard`, но компилятор видит только `Card`.
- Чтобы снова работать с методами `CreditCard`, выполняем явное приведение.

---

### 4.2. Ошибка времени выполнения
Если попытаться привести объект к несовместимому типу, будет исключение `ClassCastException`.

```java
Card card = new Card();
CreditCard wrongCast = (CreditCard) card; // Runtime error: ClassCastException
```

💡Компилятор не знает фактический тип объекта, поэтому проверка выполняется только в рантайме.

---

### 4.3. Проверка через `instanceof`
Чтобы избежать ошибки приведения, используют оператор `instanceof`.

```java
void check(Card card) {
    DebitCard d = null;
    if (card instanceof DebitCard) {
        d = (DebitCard) card;
        debitCard.someDebitMethod();
    }
}
```

💡 Проверка гарантирует, что downcast будет безопасным.
💡 JVM проверяет реальный тип объекта по ссылке и результат `card instanceof DebitCard` при `card == null` всеогда будет false, поэтому проверка на `null` не нужна и NPE не возникнет.

---

### 4.4. Сопоставление с шаблоном (Pattern Matching) Java 14+
Современный синтаксис упрощает работу: можно объявить переменную прямо в условии.

```java
void check(Card card) {
    if (card instanceof DebitCard debitCard) {
        debitCard.someDebitMethod();
    }
}
```

💡 Здесь переменная `debitCard` доступна внутри блока `if` без отдельного приведения.

---

### 4.5. Практические выводы

1.  **Приведение типа вверх** (**upcasting**) всегда безопасен.
2. **Приведение типа вниз** (**Downcasting**) требует явного приведения и может упасть с `ClassCastException`.
3. Для безопасного приведения используют `instanceof`.
4. В современных версиях Java удобнее применять **pattern matching for instanceof**.


<details>
  <summary>📒 Вопросы для самопроверки</summary>
  <p>1. В чём разница между upcasting и downcasting?</p>
  <p>2. Почему upcasting всегда безопасен?</p>
  <p>3. Что произойдёт при попытке сделать downcasting, если фактический объект — не наследник?</p>
  <p>4. Как оператор instanceof помогает избежать ошибки ClassCastException?</p>
</details>

---

## 5. Конструкторы и наследование

### 5.1. Общие правила
- У каждого класса есть свой конструктор.
- При создании объекта наследника **сначала выполняется конструктор родителя**, затем конструктора наследника.
- Если у родителя есть конструктор без аргументов, он вызывается автоматически.
- Если у родителя есть только конструкторы с параметрами, наследнику нужно явно вызвать `super(...)`.

---

### 5.2. Пример: базовый класс с конструктором

```java
public class Card {
    private String cardNumber;
    private Currency currency;
    private OffsetDateTime expiryDateTime;

    public Card(String cardNumber, Currency currency, OffsetDateTime expiryDateTime) {
        this.cardNumber = cardNumber;
        this.currency = currency;
        this.expiryDateTime = expiryDateTime;
    }
}
```

---

### 5.3. Конструктор в наследнике

```java
public class CreditCard extends Card {
    private BigDecimal creditLimit;
    private BigDecimal currentDebt;

    public CreditCard(String cardNumber,
                      Currency currency,
                      OffsetDateTime expiryDateTime,
                      BigDecimal creditLimit,
                      BigDecimal currentDebt) {
        super(cardNumber, currency, expiryDateTime); // вызов конструктора родителя
        this.creditLimit = creditLimit;
        this.currentDebt = currentDebt;
    }
}
```

💡 Замечания:
- До **Java 22 включительно**: вызов `super(...)` обязан быть **первой строкой конструктора**.
- Это гарантировало, что объект родителя будет корректно инициализирован до выполнения логики наследника.
- Начиная с **Java 22**: правило смягчено, можно писать код **до вызова `super(...)`**.  
  Это позволяет:
    - преобразовывать аргументы для конструктора родителя,
    - выполнять предварительную валидацию,
    - готовить дополнительные данные до передачи в `super(...)`.
- Если точнее, то в Java 22, 23, 24 эта фича была в стадии preview, а с выходом Java 25 получила полноценный релиз.

> 🔗 [JDK 25 with JEP 513](https://openjdk.org/jeps/513)

---

### 5.4. Порядок инициализации

1. Вызов конструктора родителя (`super(...)`).
2. Инициализация полей наследника.
3. Выполнение тела конструктора наследника.

*(В Java 22 preview, Java 25 release шаги 1–3 сохраняются, но допускается подготовительный код перед `super(...)`).*

---

### 5.5. Практические выводы

1. Конструкторы не наследуются, но вызываются цепочкой вверх по иерархии.
2. Если у родителя есть конструктор без аргументов — он вызовется по умолчанию.
3. Если конструкторов без аргументов нет — нужно явно вызвать один из доступных через `super(...)`.
4. В Java 22 и раньше `super(...)` всегда шёл первой строкой конструктора.
5. В Java 23 добавлена возможность писать код до `super(...)`, чтобы облегчить подготовку аргументов и валидацию.

<details>
  <summary>📒 Вопросы для самопроверки</summary>
  <p>1. Что произойдёт, если в родителе есть только конструктор с параметрами, а в наследнике не вызвать super(...)?</p>
  <p>2. В каком порядке выполняются конструкторы при создании объекта наследника?</p>
  <p>3. Почему до Java 22 вызов super(...) обязан был быть первой строкой?</p>
  <p>4. Что нового добавили в Java 23 в отношении вызова super(...)?</p>
  <p>5. Зачем может понадобиться писать код до super(...) в конструкторе?</p>
</details>

---

## 6. Абстрактные классы

### 6.1. Определение
- Ключевое слово `abstract` делает класс **абстрактным**.
- Абстрактный класс — это класс, от которого **нельзя создать объект напрямую**.
- Он может содержать как обычные методы, так и **абстрактные методы** (без реализации).

---

### 6.2. Пример: базовый абстрактный класс

```java
public abstract class Card {
    private String cardNumber;
    
    public abstract boolean canWithdraw();
}
```

Здесь:
- Метод `isExpired(...)` имеет реализацию.
- Метод `makePayment(...)` объявлен абстрактным — он **обязан** быть реализован в наследниках.

---

### 6.3. Реализация абстрактного метода в наследнике

```java
public class DebitCard extends Card {
    private BigDecimal balance;
    
    @Override
    public boolean canWithdraw() {
        return balance.compareTo(BigDecimal.ZERO) > 0;
    }
}
```

💡Замечания:
- `CreditCard` обязан реализовать метод `canWithdraw(...)`, иначе сам должен быть объявлен `abstract`.
- Таким образом, абстрактный класс задаёт **контракт**, а наследники реализуют детали.

---

### 6.4. Ограничения

- Экземпляры абстрактных классов создать нельзя:

```java
Card card = new Card(); // Ошибка компиляции
```

- Абстрактные методы не могут быть:
    - `private` (иначе их невозможно переопределить),
    - `static` (они связаны с экземпляром, который нельзя создать),
    - `final` (иначе переопределение невозможно).

---

### 6.5. Практические выводы

1. Абстрактный класс нужен, чтобы задать **общий контракт** и базовую реализацию.
2. Экземпляры абстрактного класса нельзя создавать напрямую.
3. Все абстрактные методы должны быть реализованы в конкретных подклассах.
4. Абстрактный класс может содержать как реализованные методы (не абстрактные), так и абстрактные, требующие реализации.

<details>
  <summary>📒 Вопросы для самопроверки</summary>
  <p>1. Можно ли создать объект абстрактного класса?</p>
  <p>2. Что обязан сделать наследник абстрактного класса?</p>
  <p>3. Почему абстрактный метод не может быть private, static или final?</p>
  <p>4. В чём отличие абстрактного метода от обычного метода с пустым телом?</p>
</details>

---

## 7. Модификатор доступа `protected`

### 7.1. Определение
- `protected` — это уровень доступа между `public` и `package-private`.
- Доступ открыт:
    - внутри того же пакета,
    - во всех классах-наследниках (даже если они в другом пакете).

### 7.2. Таблица модификаторов доступа

| Модификатор   | Класс | Пакет | Наследники | Все |
|---------------|:-----:|:-----:|:----------:|:---:|
| **public**    |  ✅   |  ✅   |     ✅     | ✅  |
| **protected** |  ✅   |  ✅   |     ✅     | 🚫  |
| *no modifier* |  ✅   |  ✅   |     🚫     | 🚫  |
| **private**   |  ✅   |  🚫   |     🚫     | 🚫  |

💡 Таблица показывает, где метод/поле доступно:
- **Класс** — внутри самого класса.
- **Пакет** — внутри всех классов одного пакета.
- **Наследники** — в подклассах.
- **Все** — доступен везде.

---

### 7.3. Пример: базовый класс с методами разного уровня доступа

```java
public abstract class Card {
    private String cardNumber;
    private Currency currency;
    private OffsetDateTime expiryDateTime;

    public void checkAndWithdraw(BigDecimal amount) {
        if (canWithdraw(amount)) {
            withdraw(amount);
        }
    }

    protected abstract boolean canWithdraw(BigDecimal amount);

    protected abstract void withdraw(BigDecimal amount);
}
```

💡 Замечания:
- Метод `checkAndWithdraw(...)` открыт для всех (`public`) — это основной API.
- Методы `canWithdraw(...)` и `withdraw(...)` — `protected`:
    - их можно вызывать только из самого класса, его наследников и внутри пакета.
    - они задают *расширяемое поведение*, которое конкретизируется в подклассах.
- Абстрактный класс может содержать `private` метод, но он должен быть реализован, то есть не быть `abstract`.

---

### 7.4. Реализация в наследнике

```java
public class CreditCard extends Card {
    private BigDecimal creditLimit;
    private BigDecimal currentDebt;

    @Override
    protected boolean canWithdraw(BigDecimal amount) {
        BigDecimal availableBalance = creditLimit.subtract(currentDebt);
        return availableBalance.compareTo(amount) >= 0;
    }

    @Override
    protected void withdraw(BigDecimal amount) {
        currentDebt = currentDebt.add(amount);
    }
}
```

💡 Здесь:
- Наследник реализует защищённые методы (`protected`), задавая конкретную логику.
- Но извне эти методы **не видны** — клиентский код работает только с `checkAndWithdraw(...)`.

---

### 7.5. Правила изменения модификаторов
- При переопределении методов **нельзя сужать** область видимости.  
  Например, переопределить `public` метод как `protected` — ошибка компиляции.
- Расширять область видимости можно: `protected` → `public`.
- Абстрактный метод не может быть `private` (иначе его невозможно переопределить).

<details>
  <summary>📒 Вопросы для самопроверки</summary>
  <p>1. Чем отличается protected от public и package-private?</p>
  <p>2. В каких случаях стоит использовать protected для методов?</p>
  <p>3. Почему абстрактный метод не может быть private?</p>
  <p>4. Можно ли расширять или сужать видимость метода при переопределении?</p>
</details>

---

## 8. Ковариантные возвращаемые типы (Covariant return types)

### 8.1. Определение
- Обычно при переопределении метода возвращаемый тип должен совпадать с типом родителя.
- Но в Java разрешено возвращать **более конкретный тип** — это называется **ковариантные возвращаемые типы (Covariant return types)**.
- Такой подход делает API удобнее: при работе с наследником не нужно явно приводить тип (downcast).

---

### 8.2. Пример

В базовом классе метод возвращает `Card`:

```java
public class Card {
    public Card renew() {
        return new Card();
    }
}
```

В наследнике класса `Card` тот же метод может возвращать объект наследника, например `CreditCard`:

```java
public class CreditCard extends Card {
    @Override
    public CreditCard renew() {
        return new CreditCard();
    }
}
```

💡 Замечания:
- Возвращаемый тип изменился с `Card` на `CreditCard`.
- Это допустимо, потому что `CreditCard` является подтипом `Card`.
- Благодаря этому можно сразу работать с объектом нужного типа, без дополнительных приведений.

<details>
  <summary>📒 Вопросы для самопроверки</summary>
  <p>1. Что означает термин "ковариантные возвращаемые типы" (Covariant return types)?</p>
  <p>2. Почему такой подход делает API удобнее?</p>
</details>

---

## 9. Модификатор `final`

### 9.1. Определение
- `final` используется для того, чтобы **запретить изменение**.
- В Java это работает на трёх уровнях:
    1. **final-переменные** — нельзя переназначить ссылку или изменить значение примитива.
    2. **final-методы** — нельзя переопределить в наследниках.
    3. **final-классы** — нельзя наследовать.

---

### 9.2. Пример: final класс

```java
public final class JuniorDebitCard {
    public void freezeCard() {}
}
```

💡 Такой класс нельзя расширить:
```java
public class SuperJuniorCard extends JuniorDebitCard { // Compilation Error
    @Override
    public void freezeCard() {}
}
```

---

### 9.3. Пример: final метод

```java
public class DebitCard extends Card {
    public final void freezeCard() {}
}
```

💡 В наследнике метод `freezeCard()` переопределить нельзя:
```java
public class JuniorDebitCard extends DebitCard { // Compilation Error
    @Override
    public void freezeCard() {}
}
```

---

### 9.4. Практические сценарии
- **final классы**: часто используют для утилитарных классов (например, `java.lang.Math`, `java.lang.String`).
- **final методы**: для методов, которые критически важны для корректности и не должны быть изменены в наследниках.
- **final переменные**: для создания неизменяемых объектов и защиты от случайного изменения состояния.

### 9.5. Выводы
1. `final` делает код более безопасным: защищает от непреднамеренного наследования или изменения логики.
2. `final` методы гарантируют, что важное поведение останется неизменным.
3. `final` классы часто служат строительным блоком для API (например, `String` — неизменяем и final).

<details>
  <summary>📒 Вопросы для самопроверки — 9. final</summary>
  <p>1. Какие три способа применения модификатора final существуют в Java?</p>
  <p>2. Зачем запрещать наследование с помощью final классов?</p>
  <p>4. Почему класс String в Java объявлен как final?</p>
</details>

---

## 10. Наследование от `Object`

### 10.1. Базовый класс всех классов
- В Java **каждый класс неявно наследуется от `java.lang.Object`**, если явно не указан другой родитель.
- Это означает, что все объекты в Java имеют набор базовых методов из `Object`.
- Переменной типа Object можно присвоить любое значение.

```java
void main() {
    DebitCard debitCard = new DebitCard();
    Object object1 = debitCard;
    Object object2 = new DebitCard();
}
```

Благодаря наследованию от класса Object, мы можем использовать его методы

```java
void main() {
    DebitCard debitCard = new DebitCard();
    Object object1 = debitCard;
    debitCard.toString(); // строковое представление объекта
    debitCard.equals(object1); // сравнение объектов на равенство
    debitCard.hashCode(); // хэш-код объекта
    debitCard.getClass(); // возвращает объект Class, описывающий тип объекта
    // и другие
}
```
---

### 10.2. Некоторые методы класса `Object`

| Метод                | Назначение                                                                 |
|-----------------------|----------------------------------------------------------------------------|
| `toString()`          | Возвращает строковое представление объекта.                               |
| `equals(Object o)`    | Проверяет равенство объектов.                                             |
| `hashCode()`          | Возвращает хэш-код объекта, используется в коллекциях (`HashMap`, `HashSet`). |
| `getClass()`          | Возвращает объект типа `Class`, описывающий данный объект.                |

---

### 10.4. Пример: переопределение `equals()`

```java
public class Card {
    private String cardNumber;

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;                // проверка на ссылку
        if (!(o instanceof Card)) return false;    // проверка типа
        Card card = (Card) o;                      // приведение к типу
        if (cardNumber == null) {                  // проверка на null полей
            return card.cardNumber == null;
        }
        return cardNumber.equals(card.cardNumber); // сравнение полей
    }
}
```

💡 Зачем это нужно:
- По умолчанию `equals()` сравнивает ссылки (как `==`).
- После переопределения можно сравнивать объекты по содержимому (`cardNumber`).

---

### 10.5. Mетод `Object.equals()`
Метод `equals()` должен придерживаться **контракта**, установленного в `Object`:

1. **Рефлексивность**:  
   `x.equals(x)` всегда возвращает `true`.

2. **Симметричность**:  
   `x.equals(y)` ⇔ `y.equals(x)`.

3. **Транзитивность**:  
   если `x.equals(y)` и `y.equals(z)`, то `x.equals(z)`.

4. **Согласованность**:  
   при повторных вызовах `x.equals(y)` результат не должен меняться, если состояние объектов не изменилось.

5. **Сравнение с null**:  
   `x.equals(null)` всегда возвращает `false`.

---

### 10.3. Пример: переопределение `toString()`

```java
public class Card {
    private Long ownerId;
    private OffsetDateTime expiryDate;
    @Override
    public String toString() {
        return "Card{" +
                "ownerId=" + ownerId +
                ", expiryDate=" + expiryDate +
                "}";
    }
}

public class DebitCard extends Card {
    public PrivilegeType privilegeType;
    public DebitCard(
            Long ownerId,
            OffsetDateTime expiryDate,
            PrivilegeType privilegeType
    ) {
        super(ownerId, expiryDate);
        this.privilegeType = privilegeType;
    }
    @Override
    public String toString() {
        return "DebitCard{" +
                "super=" + super.toString() +
                ", privilegeType=" + privilegeType +
                "}";
    }
}
```

💡 Теперь при выводе объекта:
```java
public static void main(String[] args) {
    DebitCard debitCard = new DebitCard(
            456L,
            OffsetDateTime.now(),
            PrivilegeType.PRO
    );
    System.out.println("Card state = " + debitCard);
}

```
Вывод:

```
Card state = DebitCard{super=Card{ownerId=456,
expiryDate=2025-09-15T19:52:42.657249+03:00}, privilegeType=PRO}
```

---

### 10.6. Практические выводы
1. Все классы в Java наследуют методы из `Object`.
2. На практике почти всегда переопределяют `toString()`, чтобы удобно печатать объекты.
3. `equals()` переопределяют для корректного сравнения по содержимому.
4. Без переопределения `equals()` объекты сравниваются только по ссылке.

<details>
  <summary>📒 Вопросы для самопроверки</summary>
  <p>1. От какого класса наследуются все классы в Java?</p>
  <p>2. Для чего переопределяют метод toString()?</p>
  <p>3. Чем поведение метода equals() отличается от оператора == по умолчанию?</p>
  <p>4. Какие свойства включает контракт метода equals()?</p>
</details>

---

## 11. Интерфейсы

### 11.1. Интерфейс как контракт
В интерфейсе описывается **контракт взаимодействия**: сигнатуры методов, которые должен предоставить класс-реализация. Состояние и реализация находятся в наследнике. В интерфейсе нет модификаторов доступа у методов (они `public abstract` по умолчанию).

```java
public interface SavingsAccount {
    int getInterestRate();
    BigDecimal getBalance();
    String getName();
}
```

---

### 11.2. Поля интерфейса (статические по природе)
Интерфейс может объявлять поля — они **статические** (и `public static final` неявно).

```java
public interface SavingsAccount {
    int DEFAULT_INTEREST_RATE = 15;

    int getInterestRate();
    BigDecimal getBalance();
    String getName();
}
```

---

### 11.3. Статические методы в интерфейсе
С Java 8 интерфейсы могут иметь `static`-методы.

```java
public interface SavingsAccount {
    int DEFAULT_INTEREST_RATE = 15;

    static int getDefaultInterestRate() {
        return DEFAULT_INTEREST_RATE;
    }

    int getInterestRate();
    BigDecimal getBalance();
    String getName();
}
```

---

### 11.4. Default-методы (реализация по умолчанию)
Интерфейс может предоставить **дефолтную реализацию** метода через `default`, которая при желании переопределяется в классе.

```java
public interface SavingsAccount {
    int DEFAULT_INTEREST_RATE = 15;

    static int getDefaultInterestRate() {
        return DEFAULT_INTEREST_RATE;
    }

    default int getInterestRate() {
        return DEFAULT_INTEREST_RATE;
    }
    
    BigDecimal getBalance();
    String getName();
}
```

---

### 11.5. Нельзя создавать экземпляр интерфейса
Попытка `new SavingsAccount()` — **ошибка компиляции**.

---

### 11.6. Реализация интерфейса (`implements`)
Класс реализует интерфейс через `implements` и обязан реализовать все методы без дефолтной реализации.

```java
public class FixedRateAccount 
        implements SavingsAccount 
{
    private BigDecimal balance;
    private String name;

    @Override
    public BigDecimal getBalance() {
        return balance;
    }

    @Override
    public String getName() {
        return name;
    }
}
```

---

### 11.7. Реализация нескольких интерфейсов
Класс может реализовывать **несколько интерфейсов** — нужно реализовать методы всех интерфейсов.

```java
public class FixedRateAccount 
        implements SavingsAccount, RubAccount
{
    private BigDecimal balance;
    private String name;

    @Override
    public BigDecimal getBalance() {
        return balance;
    }

    @Override
    public String getName() {
        return name;
    }
}
```

---

### 11.8. Итоги
- Интерфейс задаёт **контракт**, реализация и состояние — в классе.
- Поля интерфейса — статические; возможны `static` и `default` методы.
- Нельзя создать экземпляр интерфейса; класс обязан реализовать методы, либо быть `abstract`.
- Класс может реализовывать **несколько** интерфейсов.
- Отличие интерфейса от абстрактного класса

<details>
  <summary>📒 Вопросы для самопроверки</summary>
  <p>1. Что задаёт интерфейс и где хранится реализация и состояние?</p>
  <p>2. Какие члены (поля/методы) могут быть в интерфейсе начиная с Java 8?</p>
  <p>3. Почему нельзя написать new SavingsAccount()?</p>
  <p>4. Что обязан сделать класс, реализующий интерфейс без default-методов?</p>
  <p>5. Можно ли реализовать несколько интерфейсов в одном классе? Что из этого следует?</p>
</details>

---

## 12. Sealed классы

### 12.1. Определение
- **Sealed классы** (ограниченные классы) появились в Java 17.
- Они позволяют явно указать, какие классы могут наследовать данный базовый класс или интерфейс.
- Это повышает контроль над иерархией и делает код безопаснее и предсказуемее.

---

### 12.2. Пример sealed-класса

```java
public sealed class Card permits DebitCard, CreditCard { }

public final class DebitCard extends Card { }

public final class CreditCard extends Card { }
```

💡 Здесь:
- `Card` объявлен как `sealed`.
- Ключевое слово `permits` указывает, что наследниками могут быть только `DebitCard` и `CreditCard`.
- Классы-наследники должны быть либо `final`, либо `sealed`, либо `non-sealed`.

---

### 12.3. Варианты наследников
- **final** — дальнейшее наследование запрещено.
- **sealed** — можно продолжить ограниченное наследование, указав своих наследников.
- **non-sealed** — снимает ограничение, разрешая свободное наследование.

```java
public sealed class Card permits DebitCard, CreditCard, VirtualCard { }

public final class DebitCard extends Card { }       // наследование остановлено
public sealed class CreditCard extends Card permits GoldCreditCard { } // ограничено дальше
public non-sealed class VirtualCard extends Card { } // разрешено любому наследнику
```

---

### 12.4. Использование sealed-классов в switch
Sealed-классы хорошо работают с `switch` и pattern matching: компилятор знает полный набор наследников.

```java
static String cardInfo(Card card) {
    return switch (card) {
        case DebitCard d -> "Debit";
        case CreditCard c -> "Credit";
        case VirtualCard v -> "Virtual";
    };
}
```

💡 Преимущество: компилятор проверяет, что мы обработали **всех наследников**.

---

### 12.5. Практические выводы
1. Sealed-классы дают строгий контроль над иерархией наследования.
2. У наследников обязательно указывается модификатор: `final`, `sealed` или `non-sealed`.
3. Это делает код безопаснее и облегчает использование `switch` и pattern matching.
4. Подход используется, когда набор возможных наследников ограничен и фиксирован (например, виды карт).

<details>
  <summary>📒 Вопросы для самопроверки</summary>
  <p>1. В какой версии Java появились sealed-классы?</p>
  <p>2. Как с помощью ключевого слова permits ограничить наследников?</p>
  <p>3. Какие три модификатора доступны для наследников sealed-класса?</p>
  <p>4. Чем sealed-класс отличается от final-класса?</p>
  <p>5. Почему sealed-классы удобно использовать вместе со switch?</p>
</details>

---

## 13. Анонимные классы

### 13.1. Определение
- **Анонимный класс** — это класс без имени, который создаётся «на лету».
- Используется для локальной реализации интерфейса или абстрактного метода класса.
- Обычно применяется, когда реализация нужна **один раз** и нет смысла создавать отдельный именованный класс.

---

### 13.2. Пример: интерфейс `InterestCalculator`

```java
public interface InterestCalculator {
    BigDecimal calculateInterest(BalanceInfo balanceInfo);
}
```

---

### 13.3. Применение анонимного класса

```java
void main() {
    int exampleRate = 18;

    InterestCalculator calculator = new InterestCalculator() {
        @Override
        public BigDecimal calculateInterest(
                BalanceInfo balanceInfo
        ) {
            return calculate(balanceInfo, exampleRate);
        }
    };
}
```

💡 Здесь:
- Мы создаём новый класс, реализующий `InterestCalculator`, **прямо в месте использования**.
- Класс не имеет имени, но предоставляет реализацию метода `calculateInterest(...)`.
- Такой приём часто используют для обработчиков событий, коллбеков и тестов.

---

### 13.4. Практические выводы
1. Анонимные классы позволяют быстро реализовать интерфейс или абстрактный класс «на месте».
2. Их удобно использовать, если реализация нужна только один раз.
3. В современных версиях Java вместо анонимных классов для интерфейсов с одним методом часто применяют **лямбды** (с Java 8). Их мы рассмотрим позже в курсе.

---

<details>
  <summary>📒 Вопросы для самопроверки</summary>
  <p>1. Что такое анонимный класс?</p>
  <p>2. В каких случаях целесообразно использовать анонимный класс?</p>
  <p>3. Чем анонимный класс отличается от обычного класса с именем?</p>
  <p>4. Чем в современных версиях Java можно заменить анонимные классы для интерфейсов с одним методом?</p>
</details>

## 14. Альтернатива наследованию — композиция

### 14.1. Определение
- Помимо наследования («is-a» — *является*), в ООП есть другой важный приём — **композиция** («has-a» — *содержит*).
- При композиции класс включает в себя другой объект и использует его для реализации поведения.
- Такой подход гибче: мы можем менять поведение за счёт подмены зависимостей, а не жёсткого наследования.

---

### 14.2. Пример: использование композиции

```java
public class DepositAccount {
    private InterestCalculator interestCalculator;
    
    public BigDecimal calculateInterest(
            BalanceInfo balanceInfo,
            BigDecimal interestRate,
            Period period
    ) {
        return interestCalculator.calculateInterest(
                balanceInfo,
                interestRate,
                Period
        );
    }
}
```

💡 Здесь:
- `DepositAccount` **не наследует** `InterestCalculator`.
- Вместо этого он **содержит** поле `calculator` и делегирует ему вычисление процентов.
- Такой подход позволяет подставлять разные реализации `InterestCalculator` без изменения кода `DepositAccount`.

---

### 14.3. Когда композиция предпочтительнее наследования
1. Когда класс может использовать разные реализации поведения (через интерфейс).
2. Когда важно снизить связанность и упростить тестирование.
3. Когда наследование создаёт слишком жёсткую зависимость между классами.
4. При проектировании гибких систем по принципу **«программируй против интерфейса, а не реализации»**.

### 14.4. Практические выводы
1. **Наследование** выражает отношение «is-a» (например, `CreditCard` — это `Card`).
2. **Композиция** выражает отношение «has-a» (например, `DepositAccount` содержит `InterestCalculator`).
3. Композиция даёт больше гибкости и часто предпочтительнее наследования.  

---

## 15. Перечисления (Enum)

### 15.1. Определение и простой пример
Перечисление — это тип, задающий **ограниченный набор значений**.

```java
public enum PrivilegeType {
    STANDARD,
    PRO,
    PREMIUM
}
```

---

### 15.2. Enum с полями и методами
Перечисления могут иметь **поля, конструктор и методы**.

```java
public enum PrivilegeType {
    STANDARD(3),
    PRO(4),
    PREMIUM(5);

    private final int cashbackCategories;

    PrivilegeType(int cashbackCategories) {
        this.cashbackCategories =
                cashbackCategories;
    }

    public int getCashbackCategories() {
        return cashbackCategories;
    }
}
```

---

### 15.3. Enum, реализующий интерфейс
Enum может **реализовывать интерфейс**, предоставляя разную логику для каждого значения.

```java
public interface InterestCalculator {
    BigDecimal calculateInterest(
            BalanceInfo balanceInfo,
            BigDecimal annualInterestRate
    );
}
```

```java
public enum CapitalizationType
        implements InterestCalculator
{
    MIN_MONTHLY_BALANCE {
        public BigDecimal calculateInterest(
                BalanceInfo balanceInfo,
                BigDecimal annualInterestRate
        ) { 
            // расчет по минимальному остатку
        }
    },
    AVG_MONTHLY_BALANCE {
        public BigDecimal calculateInterest(
                BalanceInfo balanceInfo,
                BigDecimal annualInterestRate
        ) { 
            // расчет по среднему остатку 
        }
    }
}
```

---

### 15.4. Выводы
1. **Enum** — способ выразить фиксированный набор значений домена (например, тарифы/уровни привилегий).
2. Перечисления поддерживают **инкапсуляцию данных и поведения**: поля, конструкторы, методы.
3. Enum может **реализовывать интерфейсы**, позволяя каждому элементу иметь собственную реализацию контракта.

<details>
  <summary>📒 Вопросы для самопроверки</summary>
  <p>1. Зачем использовать enum вместо набора строк/чисел?</p>
  <p>2. Какие члены (поля/методы/конструкторы) можно объявлять в enum?</p>
  <p>3. Как enum может реализовывать интерфейс и зачем это нужно?</p>
</details>
