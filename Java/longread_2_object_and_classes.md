# Конспект лекции 2 — Объекты и классы

## 1. Ограничения примитивных типов

### 1.1. Почему примитивов и массивов не всегда достаточно

```java
String[] names = {"Alice", "Bob"};
int[] ages = {25, 30};
```

В этом примере связанные данные хранятся в виде параллельных массивов.

**Параллельные массивы** — это структура, где логически связанные поля хранятся раздельно и “склеиваются” индексом.
Такой подход повышает риск ошибок и усложняет эволюцию кода.

Что здесь не так с точки зрения инженерной практики:

- **Слабая связность данных.** Логически связанные поля name, age разорваны по двум структурам данных.
- **Хрупкость при изменениях.** Операции вставки, удаления, сортировки требует синхронных действий над *обоими*
  массивами.
- **Сложность инвариантов.** Индекс `i` в `names` обязан соответствовать индексу `i` в `ages` — это не выражено
  типами, а значит легко нарушается.
- **Пониженная читаемость и сопровождаемость.** Код с параллельными массивами труднее читать, тестировать, отлаживать.
- **Расширяемость.** Как только появится третье поле (например, `email`), проблем станет больше: потребуется третий массив и новый набор операций над элементами с одинаковыми индексами.

**Практический вывод:** как только у вас возникает набор связанных полей, его естественное представление — *единый
объект*, а не набор изолированных массивов. Это облегчает работу с данными.

---

### 1.2. Как возвращать составной результат из метода

```java
<NameAndAgeType> createSomethingComplex() {
    ...
    return <name_and_age >;
}
``` 

Составной результат можно вернуть в виде связанной структуры, нового типа результата работы метода.

> 💡Ключевые моменты:
>
> - **В Java нет возможностей возвращать множественные значения** "из коробки".
> - **Решение:** определить *собственный тип* результата (класс) и вернуть его. Это решает несколько проблем:
    >     - фиксирует контракт на уровне типов (self-documenting API);
>     - делает результат открытым для расширения без ломающих сигнатуру метода изменений;
>     - упрощает тестирование (можно сравнивать объекты, не придется лезть внутрь массивов).

---

<details>
  <summary> 📒 Вопросы для самопроверки</summary>
  <p>1. Почему параллельные массивы считаются анти-паттернами в промышленном коде?</p>
  <p>2. Какие риски появляются при любой сортировке/фильтрации данных в параллельных массивах?</p>
  <p>3. Как корректно вернуть из метода и имя, и возраст? Почему это решение лучше стандартных структур данных?</p>
</details>

## 2. Основные понятия

### 2.1. Что такое класс

- **Класс** в Java — это по сути пользовательский тип данных.
    - Он описывает структуру (_поля_) и поведение (_методы_) объектов.
    - Другими словами, класс — это "чертёж", по которому создаются конкретные объекты.

```java
class Person {
    String name;
    int age;
}
```

Здесь:

- `name` и `age` — **поля (fields)**, то есть переменные внутри класса, описывающие состояние объекта.
- У каждого экземпляра `Person` будут свои значения этих полей.

---

### 2.2. Объекты и экземпляры

**Объект** (object) или **экземпляр** (class instance) — это конкретный представитель класса, обладающий собственными значениями полей и доступом к методам класса.

Пример:

```java
Person alice = new Person();
```

- Оператор `new` выделяет память для объекта, инициализирует объект и возвращает ссылку на него.
- Переменная `alice` хранит эту ссылку, а не сам объект.

---

### 2.3. Доступ к полям

После создания объекта его поля можно инициализировать и читать через **оператор-точку (`.`)**:

```java
alice.name ="Alice";
alice.age =25;
```

- Оператор `.` связывает объект со свойством (полем или методом).
- Это базовый механизм инкапсуляции данных.

---

### 2.4. Пустые классы

Java позволяет объявлять классы даже без содержимого:

```java
class EmptyClass {
}
```

- Это валидный код. Иногда используется для:
    - служебных целей (например, как *маркерный класс*);
    - построения иерархий, где базовый класс не имеет своего состояния;
    - начального каркаса, который будет расширяться позже.

---

### 2.5. Практические выводы

1. **Примитивы и массивы** — это “плоские” конструкции.
2. **Классы** позволяют собрать данные в единое целое (name + age → Person).
3. **Объекты** — это конкретные **экземпляры** этих классов.
4. **оператор-точка** даёт доступ к данным объекта.
5. Даже пустой класс — корректная сущность, что подчеркивает универсальность конструкции.

---

<details>
  <summary> 📒 Вопросы для самопроверки</summary>
  <p>1. Чем класс отличается от объекта?</p>
  <p>2. Зачем нужен оператор `new`?</p>
  <p>3. Можно ли создать объект без полей? Зачем это может быть полезно?</p>
  <p>4. Что именно хранится в переменной `alice` в примере `Person alice = new Person();`?</p>
</details>

---

## 3. Поля

### 3.1. Что такое поле

- **Поле класса** (field) — это переменная, объявленная внутри класса.
- Поле описывает состояние объекта. У каждого экземпляра класса значения полей свои.

```java
class Person {
    String name;
    int age;
}
```

---

### 3.2. Инициализация и доступ

После создания объекта, доступ к полям осуществляется через dot-оператор `.`.

Пример записи и чтения:

```java
Person alice = new Person();
alice.name ="Alice";

System.out.println(alice.name); // "Alice"
System.out.println(alice.age); // значение по-умолчанию, поскольку нами не задано значение
```  

---

### 3.3. Значения по умолчанию

Если явно не задать значение, поле получает **значение по умолчанию** в момент инициализации объекта.

Пример:

```java
Person alice = new Person();
System.out.println(alice.age); // 0
```

Таблица значений по умолчанию:

| Тип данных                | Значение по умолчанию | Примечание                               |
|---------------------------|-----------------------|------------------------------------------|
| `byte`                    | `0`                   |                                          |
| `short`                   | `0`                   |                                          |
| `int`                     | `0`                   |                                          |
| `long`                    | `0L`                  | литерал с суффиксом `L`                  |
| `float`                   | `0.0f`                | литерал с суффиксом `f`                  |
| `double`                  | `0.0d`                | литерал с суффиксом `d` (часто опускают) |
| `char`                    | `'\u0000'`            | 16-битный Unicode символ                 |
| `boolean`                 | `false`               |                                          |
| `String` и другие объекты | `null`                | отсутствие ссылки                        |

---

### 3.4. Практические выводы

1. Набор полей — это “состояние объекта”.
2. Для примитивных типов на уровне класса Java всегда гарантирует начальное значение для полей.
3. Нужно осознанно инициализировать поля, чтобы избежать ошибок и неявных состояний.
4. Следует помнить разницу между примитивами и объектами: примитивы получают числовые или логические начальные значения, а объекты — `null`.

---

<details>
  <summary> 📒 Мини-вопросы для самопроверки</summary>
  <p>1. Что произойдёт, если не инициализировать поле `int`?</p>
  <p>2. Какое значение по умолчанию будет у поля `String`?</p>
</details>

---

## 4. null и NullPointerException

### 4.1. Что такое `null`

- `null` — это специальное значение для ссылочного типа, означающее “отсутствие ссылки”.
- Если поле класса или переменная ссылочного типа не инициализированы явно, они получают значение `null`.

```java
Person alice = null; // объект не создан, ссылка "пустая"
```

---

### 4.2. NullPointerException (NPE)

- Если попытаться обратиться к полю или методу через `null`, произойдет ошибка времени выполнения — **NullPointerException** (ошибка нулевого указателя).

Пример:

```java
class Person {
    String name;
    int age;
}
```
```java
Person alice = null;
System.out.println(alice.name); // runtime error: NullPointerException
```

С ошибками в Java (исключениями) мы познакомимся позднее.

---

### 4.3. Как избежать NullPointerException (NPE)

- **Проверка на `null`:**

```java
if (alice != null) {
    System.out.println(alice.name); // OK
}
```

- **Инициализация при создании.** Лучше присваивать значения полям в конструкторе, чем оставлять их `null`.
- **Явное проектирование nullable-полей.** Если поле может быть `null`, это должно быть осознанное решение, отражённое в
  документации.
- **Современные практики (для продвинутых):**
    - Использование `Optional<T>` в возвращаемых значениях методов. Мы изучим `Optional<T>` позднее в рамках курса.
    - Аннотации `@Nullable`, `@NonNull` для статического анализа. Механизм аннотаций будет освещен позднее.
    - В Java 17+ при написании бизнес-кода рекомендуют избегать “голого `null`” в API, использовать явные контракты.

---

### 4.4. Практические выводы

1. `null` — это валидное значение ссылочного типа, но оно требует осторожности.
2. Попытка обратиться к несуществующему объекту, значение которого равно `null`, вызывает `NullPointerException`.
3. Хорошая инженерная практика — минимизировать использование `null` и проектировать API так, чтобы возвращать
   гарантированные значения.
4. Там, где `null` неизбежен, следует применять проверки, аннотации и `Optional`.

---

<details>
  <summary> 📒 Вопросы для самопроверки</summary>
  <p>1. Какое значение получает поле `String`, если его не инициализировать?</p>
  <p>2. Почему обращение к методу несуществующего объекта вызывает ошибку во время выполнения, а не во время компиляции?</p>
  <p>3. Какие существуют подходы для безопасной работы с `null` в современном Java-коде?</p>
  <p>4. В чём разница между `null` и пустой строкой `""`?</p>
</details>

---

## 5. Присваивание (assignment)

### 5.1. Присваивание примитивов

- Когда присваиваем значение **примитивного типа**, копируется само значение.
- Дальнейшие изменения копии **не влияют** на оригинал.

```java
int a = 5;
int b = a;   // b получает копию значения a
b = 10;

System.out.println(a); // 5
System.out.println(b); // 10
``` 

**Вывод:** для примитивов присваивание работает по значению.

---

### 5.2. Присваивание объектов

- В Java переменная ссылочного типа хранит **ссылку** на объект.
- Когда мы делаем присваивание, копируется **ссылка**, а не сам объект.
- Это означает: две переменные указывают на один и тот же объект.

Пример:

```java
Person bob = new Person();
bob.name = "Bob";
bob.age = 30;

Person anotherBob = bob;   // копируется ссылка, оба указывают на один объект
anotherBob.age = 35;

System.out.println(bob.age); // 35
```

**Вывод:** для объектов присваивание работает "по ссылке".

---

### 5.3. Практические последствия

1. **Примитивы изолированы.** Можно копировать и изменять их независимо.
2. **Объекты совместно используются.** Изменения через одну ссылку видны через все остальные.
3. **Проблемы в коде:** могут случаться неожиданные побочные эффекты при передаче объекта в метод;

---

<details>
  <summary> 📒 Вопросы для самопроверки</summary>
  <p>1. Почему `int` ведёт себя иначе, чем `Person`, при присваивании?</p>
  <p>2. Что реально хранится в переменной объекта `bob` — сам объект или ссылка?</p>
  <p>3. Что произойдёт, если метод изменит поле объекта, переданного ему в параметре?</p>
</details>

---

## 6. Stack vs Heap

### 6.1. Два типа памяти в Java

- **Stack (стек):**
    - хранит *локальные переменные* и *информацию о вызовах методов*.
    - управляется по принципу **LIFO** (last in – first out).
    - память освобождается автоматически при выходе метода.

- **Heap (куча):**
    - хранит *объекты* и *их поля*;
    - доступ к объекту осуществляется по ссылке, которая лежит в стеке;
    - память управляется сборщиком мусора (**Garbage Collector**).

> Для углубленного изучения устройства памяти в Java: 🔗 [cтатья про Stack, Heap и не только](https://habr.com/ru/articles/510454/)

---

### 6.2. Пример: переменная и объект

```java
Person alice = new Person();
```

- В **стеке** лежит переменная `alice`, которая хранит *ссылку*.
- В **куче** создаётся объект `Person` с полями `name` и `age`.
- Если присвоить другой переменной ту же ссылку, обе переменные будут “указывать” на один объект.

---

### 6.3. Визуальная схема
//TODO 03.09.2025 ragorbatenko:
В презентации был TODO по картинке, нужно подставить сюда

---

### 6.4. Рекурсия и StackOverflowError

- Каждый вызов метода создаёт новый **кадр стека**.
- Если рекурсия бесконечна → стек переполняется → ошибка **`StackOverflowError`**.

Пример:

```java
void recurse() {
    recurse(); // бесконечная рекурсия
}
```

При запуске метод recurse() будет вызывать сам себя, пока стек не заполнится полностью.


> 💡 Для того, чтобы рекурсивные вызовы не приводили к переполнению стека и ошибке StackOverflowError, нужно **условие выхода из рекурсии**

```java
void countdown(int n) {
        if (n <= 0) { // условие выхода
            System.out.println("Старт!");
            return;
        }
        System.out.println(n);
        countdown(n - 1); // рекурсивный вызов с уменьшенным параметром
    }
```

---

### 6.5. Практические выводы

1. В **стеке** хранятся *ссылки* и локальные переменные.
2. В **куче** хранятся *объекты* и их состояние.
3. Ошибка `StackOverflowError` связана только со стеком (глубокая рекурсия, бесконечные вызовы).

---

<details>
  <summary> 📒 Вопросы для самопроверки</summary>
  <p>1. Где хранится объект класса `Person`: в стеке или в куче?</p>
  <p>2. Что реально хранит переменная `alice`?</p>
  <p>3. Почему при бесконечной рекурсии падает `StackOverflowError`, а не OutOfMemoryError?</p>
  <p>4. Что произойдёт с объектом в куче, если все ссылки на него исчезнут?</p>
</details>

---

## 7. Methods

### 7.1. Что такое метод

**Method** — это именованный блок кода, привязанный к классу, описывающий поведение объектов/класса.
Метод либо возвращает значение через `return`, либо объявляется как `void`.

### 7.2. Структура метода в Java

Метод в Java состоит из двух частей:
- **заголовок (method header)** — определяет контракт метода;
- **тело (method body)** — описывает, что именно делает метод.

Общий вид метода:

![java_methods_explained.jpg](assets/java_methods_explained.jpg)

---

#### Компоненты заголовка метода

1. **Модификатор доступа (Access modifiers)**  
   В Java существуют четыре модификатора доступа:
  - `public`
  - `private`
  - *package-private* (по умолчанию, когда модификатор не указан)
  - `protected`

   👉 Подробно разберём модификаторы позже в курсе.

2. **Другие модификаторы**
  - `static`
  - `final`
  - `abstract`
  - `synchronized`

   👉 Эти модификаторы также будут подробно рассмотрены в следующих лекциях.

3. **Возвращаемый тип (Return type)** — тип значения, которое возвращает метод (`int`, `String`, `void` и т. д.).
4. **Имя метода (Method name)** — по соглашению пишется в lowerCamelCase.
5. **Список параметров (Parameter list)** — входные данные метода, перечисленные в скобках.

---

#### Сигнатура метода

**Сигнатура метода** — это комбинация:
- имени метода,
- списка параметров (их типов и порядка).

Пример:

```java
int add(int a, int b) { // сигнатура: add(int, int)
    ...
}   

double add(double x) { // сигнатура: add(double)
    ...
}   
```

Возвращаемый тип **не входит** в сигнатуру.  
Поэтому два метода с одинаковым именем и параметрами, но разными возвращаемыми типами — **нельзя** объявить в одном классе.

📌 Это определение важно для понимания **перегрузки методов (overloading)**:  
перегружать можно только если у методов разные сигнатуры.


---

📌 Вывод:  
Метод = **заголовок** (сигнатура метода + параметры) + **тело** (логика выполнения).  
Подробный разбор модификаторов доступа и дополнительных модификаторов будет позже.

---

### 7.3. Пример класса, у которого есть метод

```java
class Person {
    String name;
    int age;

    void sayHello() {
        System.out.println("Hi, I'm " + name);
    }
}

Person p = new Person();
p.name = "Alice";
p.sayHello();      // печатает: Hi, I'm Alice
```

Метод можно вызвать, обратившись к нему через оператор `.`

---

### 7.4. Методы, меняющие состояние объекта

```java
class Counter {
    int value;

    void inc() {
        value++;
    }
}

Counter c = new Counter();
c.inc();   // value стало 1
```

Такой метод имеет **дополнительный эффект** — изменяет внутреннее состояние объекта.

---

### 7.5. Возвращаемые значения vs `void`

```java
class Person {
    String name;
    int age;

    int getAge() {
        return age;
    }

    void celebrateBirthday() {
        age++;
    }
}
```

- `getAge()` возвращает значение и может использоваться в выражениях.
- `celebrateBirthday()` ничего не возвращает, он изменяет состояние.

---

### 7.6. Параметры и семантика передачи аргументов

- Для примитивов — копируется **значение**.
- Для ссылочных типов — копируется **значение ссылки**. Это означает, что метод может менять **состояние объекта**, но
  не может “переназначить” переменную вызывающей стороны.

```java
void rename(Person p) {
    p.name = "Bob";    // меняем состояние объекта, увидим снаружи
}

void rebind(Person p) {
    p = new Person();  // переназначаем ЛОКАЛЬНУЮ копию ссылки — снаружи не видно
    p.name = "Charlie";
}
```

После `rename(alice)` имя изменится, после `rebind(alice)` — нет.

---

<details>
  <summary> 📒 Вопросы для самопроверки</summary>
  <p>1. Из каких частей состоит метод в Java?</p>
  <p>2. Что такое сигнатура метода и почему возвращаемый тип в неё не входит?</p>
  <p>3. Для чего используется ключевое слово `this` внутри методов?</p>
  <p>4. Когда имеет смысл возвращать значение из метода, а когда — изменять состояние объекта?</p>
</details>

---

## 8. Shadowing

### 8.1. Определение

**Shadowing (сокрытие переменных)** — это ситуация, когда локальная переменная или параметр метода имеет то же имя, что и поле
класса или внешняя переменная. В этом случае "ближайшая" переменная затеняет остальные.


### 8.3. Пример с полем класса и локальной переменной

```java
class A {
    int i;

    void f() {
        int i = 0; // shadowing: локальная переменная i скрывает поле i
        System.out.println(i);      // выведет локальную переменную (0)
        System.out.println(this.i); // доступ к полю класса
    }
}
```

Здесь:
- Локальная переменная `i` затеняет поле класса `i`.
- Чтобы обратиться к полю объекта, нужно использовать `this.i`.

---

### 8.4. Пример с параметром метода

```java
class A {
    int i;

    void f(int i) { // параметр i затеняет поле i
        System.out.println(i);      // напечатает значение параметра
        System.out.println(this.i); // доступ к полю класса
    }
}
```

### 8.5. Пример, который вызовет ошибку

```java
void main() {
    int i = 0;
    {
        int i = 1; // Ошибка компиляции: переменная уже объявлена
    }
}
```
- {} фигурные скобки создают **локальный блок кода**
- В Java **нельзя** объявить локальную переменную во внутреннем блоке, если переменная с таким именем уже есть во внешнем блоке.
- Компилятор не разрешит повторное объявление `i`.

---

### 8.5. Практические выводы

1. Shadowing может приводить к путанице: разработчик ожидает работу с полем, а фактически работает с локальной
   переменной или параметром.
2. Внутри методов классов для доступа к полю следует использовать `this.fieldName`, если имя совпадает с параметром или
   локальной переменной.
3. При проектировании кода желательно избегать shadowing, выбирая более говорящие имена параметров и переменных.

---

<details>
  <summary> 📒 Вопросы для самопроверки</summary>
  <p>1. Что такое shadowing и в каких случаях оно возникает?</p>
  <p>2. Чем отличается ошибка с двумя локальными переменными одного имени от затенения поля класса?</p>
  <p>3. Какой оператор в Java позволяет явно сослаться на поле объекта при shadowing?</p>
  <p>4. Почему использование одинаковых имён может ухудшать читаемость кода?</p>
</details>

---

## 9. Equality Operator

### 9.1. Сравнение примитивов

Оператор `==` при работе с примитивными типами сравнивает именно значения.

```java
int a = 42;
int b = 42;
System.out.println(a == b); // true
```

Здесь `a` и `b` содержат одно и то же значение (42), поэтому результат сравнения — `true`.

---

### 9.2. Сравнение объектов

При сравнении объектов оператор `==` проверяет, указывают ли обе переменные на **один и тот же объект в памяти** (сравниваются ссылки, а не содержимое).

Пример:

```java
Person alice = new Person(); // новый объект
Person bob = new Person();   // другой новый объект

System.out.println(alice == bob); // false
```

Хотя оба объекта могут содержать одинаковые данные, `alice` и `bob` — разные ссылки в памяти, поэтому результат `false`.

Примечание:

На самом деле, легко увидеть, что объекты alice и bob из примера выше - разные. Достаточно попробовать напечатать их.

```java
System.out.println(alice); // Person@735b5592
System.out.println(bob); // Person@58651fd0
```

В данном случае печатается имя класса + `@` + определенный код, уникальный для каждого объекта.

---

### 9.3. Метод `equals()`

Чтобы сравнить содержимое объектов, в Java используют метод `equals()`.  
По умолчанию реализация `equals()` ведет себя так же, как `==` (сравнивает ссылки).  
Чтобы сравнивать **содержимое**, метод необходимо переопределить.

Пример:

```java
class Person {
    String name;
    int age;

    public boolean equals(Person other) {
        if (this == other) {
            return true;
        }
        return this.name.equals(other.name) && this.age == other.age;
    }
}
```

```java
Person alice = new Person();
alice.name = "Alice";
alice.age = 25;

Person anotherAlice = new Person();
anotherAlice.name = "Alice";
anotherAlice.age = 25;

System.out.println(alice.equals(anotherAlice)); // true
```  

---

<details>
  <summary> 📒 Вопросы для самопроверки</summary>
  <p>1. Что сравнивает оператор `==` для примитивов?</p>
  <p>2. Что сравнивает оператор `==` для объектов?</p>
  <p>3. Зачем нужен метод `equals()`?</p>
</details>

---

## 10. Constructor

### 10.1. Зачем нужны конструкторы

- **Конструктор** — это специальный метод, который вызывается при создании объекта.
- Его задача — **инициализировать состояние объекта** (задать значения полям).
- Если создать объект без инициализации, он может оказаться в _неконсистентном состоянии_.

```java
Person p = new Person();
// p.name и p.age пока unset (null и 0)
```

---

### 10.2. Определение конструктора

- У конструктора нет возвращаемого типа (не указывается даже ключевое слово `void`).
- Имя конструктора совпадает с именем класса.

Пример:

```java
class Person {
    String name;
    int age;

    Person(String name, int age) {
        this.name = name;
        this.age = age;
    }
}

Person bob = new Person("Bob", 30);
```

> 💡 В момент, когда исполнение программы дойдет до вызова оператора `new`, у объекта вызовется соответствующий конструктор

---

### 10.3. Конструктор по умолчанию

- Если в классе **нет конструкторов**, Java добавляет **неявный конструктор без параметров**.

```java
class A {
    // компилятор сам создаст A() {}
}
``` 

- Если в классе уже объявлен хоть один конструктор, **неявный конструктор не создается**, его можно написать самостоятельно.

```java
class B {
    B(int x) { ...}
    // B() {} не будет добавлен автоматически
}
```

---

### 10.4. Перегрузка конструкторов (constructor overloading)

- В одном классе можно объявить несколько конструкторов с разными параметрами.
- Один конструктор может вызывать другой с помощью `this(...)`.

Пример:

```java
class Person {
    String name;
    int age;

    Person() {
        this("Unknown", 0); // вызов другого конструктора
    }

    Person(String name, int age) {
        this.name = name;
        this.age = age;
    }
}

Person p1 = new Person();             // name="Unknown", age=0
Person p2 = new Person("Alice", 25);  // name="Alice", age=25
```

---

### 10.5. Практические выводы

1. Конструктор гарантирует, что объект будет создан в корректном состоянии.
2. Если нужен только “пустой” объект — достаточно конструктора по умолчанию.
3. Если поля должны быть обязательными — лучше объявить собственный конструктор.
4. Перегрузка конструкторов позволяет гибко создавать объекты с разными начальными условиями.
5. Использование `this(...)` помогает избежать дублирования кода при инициализации.

---

<details>
  <summary> 📒 Вопросы для самопроверки</summary>
  <p>1. Чем отличается конструктор от обычного метода?</p>
  <p>2. Когда компилятор генерирует конструктор по умолчанию?</p>
  <p>3. Почему иногда нужно явно объявлять конструктор без аргументов?</p>
  <p>4. Как один конструктор может вызвать другой?</p>
  <p>5. Зачем перегружать конструкторы?</p>
</details>
---

## 11. Копирование объектов (copying objects)

### 11.1. `=` не копирует объекты

- Оператор `=` для объектов копирует **ссылку**, а не сам объект.
- Поэтому две переменные начинают указывать на один и тот же объект в памяти.

Пример:

```java
Person p1 = new Person();
p1.name = "Alice";

Person p2 = p1; // копируется ссылка
p2.name = "Bob";

System.out.println(p1.name); // "Bob"
```

Здесь изменилось поле объекта через `p2`, но результат виден и через `p1`, потому что это один объект.

---

### 11.2. Конструктор копирования (copy constructor)

Чтобы создать **независимую копию** объекта, используют *конструктор копирования*:

```java
class Person {
    String name;
    int age;

    Person(Person other) {
        this.name = other.name;
        this.age = other.age;
    }
}
```

Теперь:

```java
Person alice = new Person();
alice.name = "Alice";
alice.age = 25;

Person copy = new Person(alice);
System.out.println(copy.name); // "Alice"
```

---

### 11.3. Проблема поверхностного копирования (shallow copy)

Пример из слайда:

```java
class Person {
    String name;
    int age;
    Address address;

    Person(Person other) {
        this.name = other.name;
        this.age = other.age;
        this.address = other.address; // копируется ссылка!
    }
}
```

- Здесь `address` указывает на тот же объект, что и в `other`.
- Изменения в `copy.address` будут видны и в `original.address`.

---

### 11.4. Глубокое копирование (deep copy)

- Чтобы создать **полную независимость**, нужно копировать вложенные объекты тоже.
- В примере выше нужно создать новый объект `Address`, скопировав его содержимое.

Пример:

```java
class Address {
    String city;

    Address(Address other) {
        this.city = other.city;
    }
}

class Person {
    String name;
    int age;
    Address address;

    Person(Person other) {
        this.name = other.name;
        this.age = other.age;
        this.address = new Address(other.address); // глубокая копия
    }
}
```

---

### 11.5. Практические выводы

1. Присваивание объектов (`=`) всегда копирует **ссылку**, а не данные.
2. Чтобы создать новый объект с теми же данными, нужен **copy constructor**, хотя есть и другие способы.
3. **Shallow copy** копирует только ссылки на вложенные объекты → изменения будут разделяться.
4. **Deep copy** создаёт полные независимые дубликаты, но может быть дорогим по ресурсам.

---

<details>
  <summary> 📒 Вопросы для самопроверки</summary>
  <p>1. Что именно копируется при `Person p2 = p1;`?</p>
  <p>2. Чем отличается поверхностное от глубокого копирования?</p>
  <p>3. Как реализовать copy constructor для класса с вложенными объектами?</p>
  <p>4. Какие проблемы могут возникнуть при неправильном копировании объектов?</p>
  <p>5. Почему глубокое копирование может быть затратным?</p>
</details>

---

## 12. String Interning

### 12.1. Особенности строк в Java

- **String** — неизменяемый класс (**immutable**).
- Благодаря этому JVM может безопасно переиспользовать строки.

---

### 12.2. String Pool

- Все строковые **литералы** автоматически помещаются в специальный пул строк (**String Pool**), находящийся в heap.
- При повторном использовании литерала будет возвращена ссылка на уже существующую строку.

Пример:

```java
String s1 = "hello";
String s2 = "hello";

System.out.println(s1 == s2); // true
```

Здесь `s1` и `s2` указывают на один и тот же объект из пула.

---

### 12.3. Создание строки через `new`

- Если явно использовать `new String("...")`, объект будет создан **вне пула**, даже если строка с таким же содержимым
  уже есть.

Пример:

```java
String s3 = new String("hello");

System.out.println(s3 == s1); // false
```

---

### 12.4. Метод `intern()`

- Чтобы вернуть строку в пул, используется метод `intern()`.
- Если в пуле уже есть строка с таким содержимым, будет возвращена ссылка на неё.

Пример:

```java
String s4 = s3.intern();
System.out.println(s4 == s1); // true
```

---

### 12.5. Практические выводы

1. Литералы строк всегда попадают в String Pool.
2. Оператор `==` сравнивает ссылки, поэтому с литералами может возвращать `true`.
3. Для сравнения содержимого строк по символам следует использовать `equals()`.
4. Метод `intern()` позволяет экономить память, но злоупотреблять им не стоит (может перегружать пул).
5. Неизменяемость строк обеспечивает безопасность пула: одна строка может использоваться везде без риска изменения.

---

<details>
  <summary> 📒 Вопросы для самопроверки</summary>
  <p>1. Почему строки сделали immutable?</p>
  <p>2. Что такое String Pool?</p>
  <p>4. Что делает метод `intern()`?</p>
  <p>5. Почему `==` иногда работает для строк, а иногда нет?</p>
</details>
