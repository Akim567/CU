## Введение: Зачем нужны аннотации в Java?

Аннотации в Java — это механизм для добавления кода **дополнительных метаданных**, которые описывают свойства и назначение классов, методов, параметров и других элементов программы.

Сами аннотации **не содержат исполняемого кода** и **не изменяют поведение напрямую**, но позволяют инструментам — компилятору, фреймворкам, библиотекам рантайма и annotation processor’ам — читать эти метаданные и на их основе **проверять корректность кода, генерировать вспомогательные конструкции или управлять выполнением программы**.

Благодаря этому аннотации лежат в основе множества механизмов современной Java-экосистемы:

- компилятор может проверять правильность реализации (например, через `@Override` или `@FunctionalInterface`);
- IDE использует аннотации для подсказок, навигации и предупреждений (`@Deprecated`, `@SuppressWarnings`);
- annotation processors генерируют код во время компиляции (Lombok, MapStruct);
- фреймворки динамически меняют поведение программы, ориентируясь на аннотации (`@Test` в JUnit, `@Service` / `@Component` в Spring, `@Entity` в JPA).

В этом конспекте мы разберём:
- что представляет собой аннотация и какую роль она играет;
- кто и как использует аннотации;
- как объявить собственную аннотацию;
- мета-аннотации (`@Target`, `@Retention`, `@Documented`, `@Inherited`, `@Repeatable`);
- параметры аннотаций, их типы и значение параметра `value`.

Аннотации — это удобный декларативный способ сообщать инструментам о намерениях разработчика. Всё остальное — результат работы этих инструментов.

## 1. Что такое аннотации

Аннотации — это специальная языковая конструкция Java, позволяющая прикреплять к элементам программы структурированные метаданные. Эти метаданные затем могут быть использованы компилятором, IDE, инструментами анализа кода, annotation processor’ами и фреймворками.

## 1.1. Аннотация как языковая конструкция

Аннотация записывается в виде:

```java
@AnnotationName
@AnnotationName(parameters)
```

Аннотация может быть размещена на различных элементах программы:

- классах, интерфейсах, enum’ах, record’ах;
- методах и конструкторах;
- полях;
- параметрах методов и конструкторов;
- локальных переменных;
- типах (Type Use Annotations, Java 8+).

Пример:

```java
@Override
public String toString() { return "..."; }

@Deprecated(since = "20")
public void oldApi() { }

public class User {
    @SuppressWarnings("unused")
    private String internalField;

    public void send(@Nonnull String email, @Valid Address address) { }
}
```

> Примечание: аннотации `@Nonnull` и `@Valid` не входят в стандарт JDK. Их предоставляют сторонние библиотеки (например, `@Nonnull` — JSR‑305/JetBrains/Checker Framework, `@Valid` — Jakarta Bean Validation). Здесь они используются только как иллюстрация «аннотаций на параметрах». В собственных примерах в проекте вы можете применять как стандартные, так и пользовательские аннотации.

## 1.2. Аннотация — это тип

Аннотация в Java — это специальный вид типа. Она объявляется через ключевое слово `@interface`:

```java
public @interface MyAnnotation { }
```

Аннотация может иметь параметры, которые задаются при использовании. Полную структуру объявления мы рассмотрим позже.

## 1.3. Где можно применять аннотации

Аннотации могут быть применены к:

- типам (классам, интерфейсам, enum, record);
- методам и конструкторам;
- полям и переменным;
- параметрам;
- использованию типов в сигнатурах.

Пример аннотации на типе:

```java
List<@Nonnull String> emails;
```

## 1.4. Виды аннотаций по структуре

### Маркерные аннотации

Не имеют параметров, важен факт наличия:

```java
@Override
@FunctionalInterface
```

### Аннотации с параметрами

Имеют элементы (параметры), которые задаются при использовании:

```java
@Deprecated(since = "20", forRemoval = true)
public void oldMethod() { }

@SuppressWarnings("unchecked")
void useLegacyApi() { }
```

## 1.5. Основные встроенные аннотации JDK

- `@Override` — проверка корректности переопределения метода.
- `@Deprecated` — пометка устаревших элементов.
- `@SuppressWarnings` — подавление предупреждений компилятора.
- `@FunctionalInterface` — проверка, что интерфейс содержит один абстрактный метод.
- `@SafeVarargs` — подавление предупреждений при работе с varargs и generics.

---

<details>
  <summary>📒 Вопросы для самопроверки</summary>

1. Где в Java можно размещать аннотации?
2. Почему аннотация считается типом? Как она объявляется?
3. Чем маркерная аннотация отличается от аннотации с параметрами?

</details>

## 2. Кто использует аннотации

Аннотации сами по себе пассивны: они лишь хранят метаданные. Но когда эти метаданные читают инструменты — компилятор, IDE, процессоры аннотаций, фреймворки или библиотеки анализа байткода — аннотация начинает влиять на работу программы. В этом пункте разберём, кто именно использует аннотации и как.

## 2.1. Компилятор Java

Компилятор (`javac`) использует аннотации для проверки корректности кода.

### Проверка корректности переопределения

```java
@Override
private void nonExistentMethod() {}
```

Компилятор выдаст ошибку:

```
java: method does not override or implement a method from a supertype
```

Аннотация сообщает компилятору, что метод **должен** переопределять метод родительского класса. Если это не так — компиляция прерывается.

### Проверка функционального интерфейса

```java
@FunctionalInterface
public interface Supplier<T> {
    T get();
    T getDefault(); // Ошибка компиляции: больше одного абстрактного метода
}
```

Аннотация требует, чтобы в интерфейсе был ровно один абстрактный метод.

## 2.2. Инструменты разработки (IDE)

IDE используют аннотации для улучшения качества разработки: подсветки, предупреждений, навигации.

Пример использования:

```java
@Deprecated(since = "20")
public URL(String spec) throws MalformedURLException {
    this(null, spec);
}
```

IDE автоматически:
- подсвечивает использование устаревшего API,
- предлагает замену,
- показывает предупреждения.

Такая реакция — результат работы IDE, а не самой аннотации.

## 2.3. Annotation Processors (процессоры аннотаций)

Процессоры аннотаций работают **во время компиляции**. Они могут анализировать и дополнять код.

Это отдельный механизм Java, позволяющий создавать инструменты вроде Lombok и MapStruct.

Пример:

```java
import lombok.Getter;

public class User {
    @Getter
    private Long id;

    public User(Long id) {
        this.id = id;
    }
}
```

Lombok-аннотация `@Getter` активирует процессор аннотаций, который генерирует метод:

```java
public Long getId() { return this.id; }
```

## 2.4. Анализаторы байткода

Байткод — это формат `.class` файлов, который исполняет JVM. Существуют библиотеки (например, ByteBuddy или ASM), которые умеют:

- читать байткод,
- анализировать его структуру,
- добавлять или изменять методы,
- создавать новые классы.

Эти инструменты используют аннотации как подсказки. Например, если аннотация доступна в `.class` файле (Retention.CLASS), библиотека может прочитать её и изменить поведение программы на уровне байткода.

Проще говоря:
> Анализатор байткода — это инструмент, который читает и изменяет **скомпилированный** код, а не исходники.

## 2.5. Runtime-фреймворки

Фреймворки, работающие во время выполнения программы, считывают аннотации через механизм reflection.

Пример:

```java
@Test
void shouldLinesBeEqual_whenConstructedFromTheSamePoints() {
    // ...
}
```

JUnit:
1. находит методы с аннотацией `@Test`,
2. вызывает их автоматически.

Другие примеры:
- Фреймворк Spring ищет `@Component`, `@Service`, `@Configuration` для создания бинов.
- ORM фреймворк Hibernate читает `@Entity`, `@Id`, `@Column` для ORM-маппинга.

> Чтобы фреймворк мог увидеть аннотацию во время выполнения, она должна иметь `RetentionPolicy.RUNTIME`. Мы рассмотрим этот механизм более подробно далее.

---

<details>
  <summary>📒 Вопросы для самопроверки</summary>

1. Почему некорректный `@Override` вызывает ошибку компиляции?
2. Какую роль IDE играет при использовании аннотации `@Deprecated`?

</details>

## 3. Простейшая аннотация и использование

Чтобы понять механизм аннотаций, начнём с их минимального варианта: простой пользовательской аннотации без параметров и без дополнительных ограничений. Такой пример хорошо показывает, что аннотация — это обычный тип Java со специальным синтаксисом, который можно прикреплять к элементам программы.

## 3.1. Как объявить собственную аннотацию

Аннотация объявляется с помощью ключевого слова `@interface`. Формально это особый вид интерфейса, в котором вместо методов описываются элементы (параметры аннотации). Но в простейшем случае параметров нет:

```java
public @interface CustomAnnotation {
}
```

Приведенный пример называется *маркерной аннотацией*. Её наличие сообщает инструментам или фреймворкам, что аннотируемый элемент обладает определённым свойством, которое важно при анализе или обработке кода.

## 3.2. Применение аннотации

Аннотация используется так же, как стандартные, уже встречавшиеся нам аннотации:

```java
@CustomAnnotation
public class CustomClass {
}
```

> Ограничения применения и области видимости аннотаций мы рассмотрим позже.

## 3.3. Аннотация — это тип

У любой аннотации есть собственный тип, доступный через `.class`:

```java
Class<CustomAnnotation> type = CustomAnnotation.class;
```

Аннотации можно считывать через механизм reflection:

```java
CustomAnnotation annotation = CustomClass.class.getAnnotation(CustomAnnotation.class);
```

## 3.4. Чем полезны простые (маркерные) аннотации

Даже без параметров аннотации широко используются благодаря своей выразительности:

- помогают статическому анализу (`@Override`),
- обозначают специальные свойства кода (`@FunctionalInterface`, `@SafeVarargs`),
- служат флажками для инструментов (`@Test`, `@Entity`, `@Component`),
- позволяют упрощать конфигурацию: инструмент «сам» определяет нужное поведение, увидев аннотацию.

Маркерные аннотации — это декларативный способ выразить утверждение о коде: «этот элемент обладает определённым свойством, и инструмент должен учесть это при обработке».

---

<details>
  <summary>📒 Вопросы для самопроверки</summary>

1. Что означает запись `public @interface MyAnnotation { }`?
2. Чем аннотация отличается от обычного интерфейса?
3. Что такое маркерная аннотация?

</details>

## 4. Мета-аннотации

Мета-аннотации — это аннотации, которые применяются к другим аннотациям. Они определяют поведение аннотации: где её можно использовать, видна ли она в runtime, попадёт ли в документацию, наследуется ли она и можно ли аннотацию применять несколько раз в одном месте.

Основные мета-аннотации Java:
- `@Documented`
- `@Target`
- `@Retention`
- `@Inherited`
- `@Repeatable`

---

## 4.1. @Documented

Указывает, должна ли аннотация отображаться в Javadoc.

По умолчанию аннотации **не появляются** в документации. Это может быть неудобно, если аннотация влияет на поведение класса или является частью публичного API.

Пример:

```java
@Documented
public @interface MyAnnotation { }
```

Если аннотировать класс:

```java
@MyAnnotation
public class User { }
```

То в сгенерированном Javadoc будет видно, что `User` помечен `@MyAnnotation`. Это полезно, когда аннотация — часть внешнего контракта.

---

## 4.2. @Target

Ограничивает, **к чему можно применять аннотацию**.

Исходник `@Target`:

```java
@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.ANNOTATION_TYPE)
public @interface Target {
    ElementType[] value();
}
```

### Основные значения ElementType

| ElementType        | Где применяется                               | Пример |
|--------------------|-----------------------------------------------|--------|
| `TYPE`             | классы, интерфейсы, enum, record               | `@Service class A {}` |
| `FIELD`            | поля                                           | `@Inject String s;` |
| `METHOD`           | методы                                         | `@Test void m()` |
| `PARAMETER`        | параметры методов/конструкторов                | `void f(@Nonnull String s)` |
| `CONSTRUCTOR`      | конструкторы                                   | `@Autowired A()` |
| `LOCAL_VARIABLE`   | локальные переменные                           | `@SuppressWarnings("unused") int x;` |
| `ANNOTATION_TYPE`  | на другие аннотации                            | `@Target(...)` |
| `PACKAGE`          | пакеты                                         | `@Deprecated package com.demo;` |
| `TYPE_PARAMETER`   | параметры типов                                | `class Box<@MyAnno T> {}` |
| `TYPE_USE`         | любое использование типа                       | `List<@Nonnull String>` |
| `MODULE`           | модули                                         | `@Deprecated module m {}` |
| `RECORD_COMPONENT` | record-компоненты                              | `record Point(@Positive int x)` |

Пример ограничения:

```java
@Target(ElementType.METHOD)
public @interface LogInvocation { }
```

Такую аннотацию можно навешивать **только на методы**.

---

## 4.3. @Retention

Определяет, на каком этапе жизненного цикла программы аннотация должна быть доступна — только компилятору, инструментам, читающим .class-файлы, или коду во время выполнения.

Исходник `@Retention`:

```java
@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.ANNOTATION_TYPE)
public @interface Retention {
    RetentionPolicy value();
}
```

### Политики хранения аннотаций

| Политика | Хранится где | Кто может видеть |
|----------|--------------|------------------|
| `SOURCE` | только в исходниках | компилятор и annotation processors |
| `CLASS`  | в .class, но не видно в runtime | анализаторы байткода |
| `RUNTIME` | хранится в .class и доступна в runtime | reflection, Spring, JUnit |

По умолчанию (если `@Retention` не указана) применяется политика `CLASS`. Это означает, что аннотация попадёт в `.class`-файл, но через reflection её не будет видно.

### Пример видимости аннотаций

```java
@Retention(RetentionPolicy.SOURCE)
@interface SourceMarker {}

@interface ClassMarker {}

@Retention(RetentionPolicy.RUNTIME)
@interface RuntimeMarker {}

@SourceMarker
@ClassMarker
@RuntimeMarker
class A {}

void someMethod(A instance) {
    boolean sourceMarkerPresent = instance.getClass().isAnnotationPresent(SourceMarker.class);   // false

    boolean classMarkerPresent = instance.getClass().isAnnotationPresent(ClassMarker.class);     // false

    boolean runtimeMarkerPresent = instance.getClass().isAnnotationPresent(RuntimeMarker.class);   // true
}
```

Объяснение:
- При указании Retention `SOURCE`, аннотация удаляется после компиляции и reflection ее не видит.
- При указании Retention `CLASS`, аннотация есть в `.class`, но reflection её не видит.
- При указании Retention `RUNTIME`, аннотация доступна во время выполнения программы.

---

## 4.4. @Inherited

Позволяет указать, что аннотация автоматически наследуется подклассами.

```java
@Inherited
public @interface Secured { }
```

```java
@Secured
public class BaseController { }

public class UserController extends BaseController { }
```

Теперь `UserController` считается аннотированным `@Secured`.

### Где реально применяется @Inherited

- В фреймворках для случаев, когда свойства должны наследоваться вниз по иерархии классов;
- Когда аннотация описывает роль/тип компонента, а классы-потомки должны наследовать это поведение.

**Важно:** работает только для аннотаций на классах; не распространяется на методы, поля и конструкторы, и не наследуется через интерфейсы (если класс реализует интерфейс, аннотации интерфейса не «передаются» классу).

---

## 4.5. @Repeatable

Позволяет использовать одну аннотацию несколько раз на одном элементе.

```java
@Repeatable(Schedules.class)
public @interface Schedule {
    String value();
}

public @interface Schedules {
    Schedule[] value();
}
```

Использование:

```java
@Schedule("daily")
@Schedule("hourly")
class BackupService { }
```

### Когда реально полезно

- несколько ограничений или правил (`@Tag` в JUnit 5);
- несколько расписаний (`@Schedule` в Jakarta EE);
- несколько политик безопасности (Spring Security);
- конфигурации, где нужно указать несколько повторяющихся параметров.

---

<details>
  <summary>📒 Вопросы для самопроверки</summary>

1. Зачем нужна `@Documented`?
2. Какие элементы можно ограничить через `@Target`?
3. Чем различаются `SOURCE`, `CLASS` и `RUNTIME`?
4. Почему аннотация может быть не видна через reflection?
5. Когда `@Inherited` полезен на практике?

</details>

## 5. Параметры аннотаций

Аннотации могут содержать параметры (elements). Параметры позволяют передавать аннотации дополнительные настройки, которые инструмент или фреймворк может использовать при обработке кода. Параметры объявляются как методы без тела внутри аннотации.

## 5.1. Объявление параметров

```java
@Target({ElementType.TYPE, ElementType.METHOD})
@Retention(RetentionPolicy.RUNTIME)
public @interface CustomAnnotation {
    int value();              // обязательный параметр
    String anotherValue();    // обязательный параметр
    Status status();          // обязательный enum-параметр
}
```

Каждый параметр по сути это метод без аргументов и тела, возвращающий допустимый тип и определяющий обязательность параметра для аннотации.

## 5.2. Допустимые типы параметров

Java строго ограничивает, какие типы можно использовать в качестве параметров для аннотаций.

**Разрешённые типы:**
- примитивы (`int`, `double`, `boolean`, ...),
- `String`,
- `enum`,
- `Class<?>`,
- другие аннотации,
- массивы всех перечисленных типов.

**Запрещённые типы:**
- произвольные объектные типы,
- коллекции (`List`, `Set`, `Map`),
- generic-типы,
- значение `null`.

Ограничение сделано для того, чтобы параметры были неизменяемыми и сериализуемыми в `.class` файлах.

## 5.3. Значения по умолчанию

Параметр аннотации можно сделать необязательным, задав значение по умолчанию:

```java
public @interface CustomAnnotation {
    int value();
    String anotherValue() default "";
    Status status() default Status.OK;
}
```

Параметры `anotherValue` и `status` можно теперь не указывать.

## 5.4. Использование аннотации с параметрами

### Специальное имя `value`

Если параметр называется `value`, его можно указывать без имени:

```java
@CustomAnnotation(42)
public class CustomClass { }
```

То же в развёрнутом виде:

```java
@CustomAnnotation(value = 42)
```

### Несколько параметров

Если нужно указать другие параметры, их записывают явно:

```java
@CustomAnnotation(value = 42, anotherValue = "foo")
public class CustomClass { }
```

Все параметры:

```java
@CustomAnnotation(value = 42, anotherValue = "foo", status = Status.NOT_OK)
public class CustomClass { }
```

Параметры с `default` можно не указывать.

## 5.5. Чтение параметров через Reflection

Аннотация должна иметь `@Retention(RetentionPolicy.RUNTIME)`, чтобы параметры были доступны во время выполнения:

```java
@CustomAnnotation(42)
public class CustomClass { }

void f(CustomClass instance) {
    CustomAnnotation annotation = instance.getClass().getAnnotation(CustomAnnotation.class);

    int value = annotation.value();                     // 42
    String anotherValue = annotation.anotherValue();    // ""
    Status status = annotation.status();                // OK (значение по умолчанию)
}
```

---

<details>
  <summary>📒 Вопросы для самопроверки</summary>

1. Какие типы могут использоваться как параметры аннотаций?
2. Почему параметры аннотаций не могут иметь произвольные объектные типы или коллекции?
3. Для чего используется `default` в параметрах аннотаций?

</details>





