# Конспект лекции 3 — Классы: расширенные возможности

## Введение

В предыдущей лекции мы познакомились с базовыми принципами работы с объектами в Java:
- разобрали, что такое **класс** и **объект**;
- научились работать с **полями** и **методами**;
- изучили конструкторы и перегрузку;
- увидели, как работает присваивание объектов и копирование (shallow vs deep copy);
- обсудили области памяти (**stack vs heap**) и поведение ссылок;
- разобрали модификаторы доступа и основы инкапсуляции.

Теперь мы будем двигаться **глубже** и разберём особенности языка, которые помогают строить более надёжные и удобные в использовании классы:

- использование ключевого слова **final** и способы защиты состояния объектов от изменений;
- построение **неизменяемых (immutable) классов ** и применение **записей (records)** (начиная с Java 16);
- роль модификатора **static** для полей и методов, статических блоков и фабричных методов;
- организация кода через **пакеты (packages)** и работу с `import`;
- разные виды **вложенных (nested) классов**: static nested, inner, local.

Если во 2-й лекции мы отвечали на вопрос *«как устроены классы и объекты в принципе»*, то в 3-й лекции мы сосредоточимся на *«какие у классов есть особые возможности и идиомы, которые важно знать для промышленного Java-кода»*.

## 1. Модификатор Final и неизменяемость (Immutability)

### 1.1. Неизменяемые поля (Final Fields)

Иногда данные объекта должны быть установлены один раз и **никогда не меняться**.  
Пример: год рождения человека (`birthYear`) — логически неизменяемое значение.

```java
class Person {
    String name;
    int birthYear;
}

Person alice = new Person();
alice.birthYear = 2000;
alice.birthYear = 2020; // Логически неверно — поле изменилось
```

Решение — использовать модификатор `final`, который запрещает повторное присваивание:

```java
class Person {
    final int birthYear;
    String name;

    Person(String name, int birthYear) {
        this.name = name;
        this.birthYear = birthYear;
    }
}

Person alice = new Person("Alice", 2000);
alice.birthYear = 2020; // CE: поле final, изменить нельзя
```

---

### 1.2. Final локальные переменные и параметры

`final` можно применять не только к полям класса, но и к переменным внутри методов и к параметрам.

```java
void greet(final String name) {
    // name = "Bob"; // CE
    System.out.println("Hello, " + name);
}
```

```java
void handleInsideLoop() {
    final int limit = 42;
    for (int i = 0; i < limit; i++) {
        ...
    }
    // limit = 43; // CE
}
```

Таким образом, `final` защищает переменные от переопределения внутри метода.

---

### 1.3. Final и массивы

Важно понимать: `final` защищает **ссылку**, а не содержимое.  
Пример из презентации:

```java
final int[] a = {1, 2, 3};
// a = new int[4]; // CE
a[0] = 99; // допустимо
```

Здесь:
- переприсвоить массив нельзя (ошибка компиляции),
- но элементы внутри массива изменяются свободно.

Вывод: `final` ≠ immutability.

---

### 1.4. Проблема мутации состояния извне

Даже если поле объявлено `final`, внешние ссылки могут позволять менять содержимое.

```java
class Team {
    final int[] scores;

    Team(int[] scores) {
        this.scores = scores; // кто-то может поменять массив снаружи
    }
}
```

Чтобы защитить внутреннее состояние, нужно использовать **defensive copy**:

```java
class Team {
    final int[] scores;

    Team(int[] scores) {
        this.scores = scores.clone(); // или Arrays.copyOf(scores, scores.length)
    }
}
```

Альтернативный вариант — инициализировать массив самим:

```java
class Team {
    final int[] scores;

    Team(int length) {
        this.scores = new int[length];
    }
}
```

---

### 1.5. Проблема с геттером

Даже при `private final` поле можно случайно «пробросить» доступ к нему через геттер:

```java
class Team {
    private final int[] scores;

    Team(int length) {
        this.scores = new int[length];
    }

    int[] getScores() {
        return scores;
    }
}

void someMethod(Team team) {
    int[] scores = team.getScores();
    scores[0] = 42; // меняем внутреннее состояние извне
}
```

Решение — возвращать копию:

```java
class Team {
    private final int[] scores;

    Team(int length) {
        this.scores = new int[length];
    }

    int[] getScores() {
        return scores.clone();
    }
}
```

Теперь при изменении копии внутреннее состояние объекта не затрагивается.

---

### 1.6. Неизменяемые классы (Immutable Classes)

Чтобы гарантировать неизменяемость, делают все поля `private final`, инициализацию выполняют в конструкторе, а наружу предоставляют только методы для чтения:

```java
class Person {
    private final String name;
    private final int birthYear;

    Person(String name, int birthYear) {
        this.name = name;
        this.birthYear = birthYear;
    }

    String name() {
        return name;
    }

    int birthYear() {
        return birthYear;
    }
}
```

В Java 16+ появилась специальная конструкция — `record`, которая упрощает создание immutable-классов (подробнее — в следующем разделе).

---

<details>
  <summary> 📒 Вопросы для самопроверки</summary>
  <p>1. В чём разница между `final` и immutability?</p>
  <p>2. Что произойдёт при попытке переприсвоить значение `final` переменной?</p>
  <p>3. Можно ли изменить содержимое массива, объявленного как `final`?</p>
  <p>4. Зачем использовать defensive copy в конструкторах и геттерах?</p>
  <p>5. Как гарантировать, что объект будет полностью immutable?</p>
</details>

## 2. Записи (Records)

### 2.1. Что такое Record

Начиная с версии Java 16 в язык была добавлена новая функциональность `Records`. Это специальный вид класса, появившийся в Java 16.  
Он предназначен для создания **неизменяемых (immutable) структур данных** с минимальным объемом шаблонного кода.

```java
record Person(String name, int birthYear) {
    // тело record
}
```

Что генерируется автоматически:
- конструктор со всеми полями,
- геттеры (методы с именами полей),
- `equals()`, `hashCode()`,
- `toString()`.

Пример использования:

```java
Person p = new Person("Alice", 2000);
System.out.println(p.name());       // Alice
System.out.println(p);              // Person[name=Alice, birthYear=2000]
```

---

### 2.2. Канонический конструктор

```java
record Person(String name, int birthYear) {
    Person(String name, int birthYear) {
        // проверки входных параметров конструктора
        this.name = name;
        this.birthYear = birthYear;
    }
}
```
Приведенный выше конструктор автогенерируется при использовании record, он называется `каноническим`.
Канонический конструктор принимает параметры, которые называются так же как и поля record, и передает полям значения соответствующих параметров. Автоматически сгенерированный конструктор можно переопределить, чтобы добавить проверки или логику, как показано на примере.

---

### 2.3. Компактный конструктор

Если нужны проверки, но без явного списка параметров, используют компактный синтаксис:

```java
record Person(String name, int birthYear) {
    Person {
        // проверки входных параметров конструктора
    }
}
```

Здесь параметры конструктора автоматически доступны в теле метода, а поля инициилизируются параметрами после выхода из тела конструктора.
Кроме того, параметры можно изменить в теле, после чего поля проинициализируются новыми значениями:

```java
record Person(String name, int birthYear) {
    Person {
        name = name.toUpperCase();
    }
}

Person alice = Person("Alice", 2000);
System.out.println(alice.name()); // "ALICE"
```

При этом в теле компактного конструктора нельзя как явно проинициализировать поля, так и обращаться к их значениям. 
Последний запрет аналогичен правилам для обычных классов: доступ к `final` полям невозможен до их инициализации.

```java
record Person(String name, int birthYear) {
    Person {
        // this.name = ""; // CE
        // System.out.println(this.name); // CE
    }
}
```

Также можно объявлять конструкторы без параметров или с параметрами, отличными от полей. 
Такие конструкторы должны вызывать другие конструкторы и в конечном итоге приводить к вызову канонического конструктора:

```java
record Person(String name, int birthYear) {
    Person(String name) {
        this(name, 0); // call canonical constructor
    }
    
    Person() {
        this("default_name"); // call previous constructor
    }
}
```

---

### 2.4. Отличия records от классов

- **Immutable по умолчанию**: все поля `private final`, без сеттеров.
- **Value-based**: сравнение через `equals()` по содержимому.
- Удобны для DTO (Data Transfer Object — объекты для переноса данных между слоями приложения), результатов методов, ключей в коллекциях.
- Есть и другие отличия, о которых мы поговорим в следующих лекциях.
---

<details>
  <summary> 📒 Вопросы для самопроверки</summary>
  <p>1. Что автоматически генерирует `record`?</p>
  <p>2. Чем отличается canonical constructor от compact constructor?</p>
  <p>3. Почему records хорошо подходят для DTO?</p>
  <p>4. Можно ли унаследовать record от другого класса?</p>
  <p>5. Чем record отличается от вручную написанного immutable-класса?</p>
</details>

## 3. Модификатор static

### 3.1. Проблема подсчёта объектов

Если хранить счётчик как **поле экземпляра**, он не будет отражать общее количество созданных объектов:

```java
class Person {
    int counter = 0;

    Person() {
        this.counter++;
    }
}

Person p1 = new Person();
Person p2 = new Person();
System.out.println(p1.counter); // 1
System.out.println(p2.counter); // 1
```

Каждый объект хранит свою копию поля `counter`, поэтому подсчёт неверный.

---

### 3.2. Статические поля (Static Fields)

Чтобы иметь **общее поле для всех объектов класса**, используют модификатор `static`:

```java
class Person {
    static int counter = 0;

    Person() {
        counter++;
    }
}
```

Теперь `counter` принадлежит классу, а не экземпляру.  
Доступ возможен даже без создания объекта:

```java
System.out.println(Person.counter);
```

---

### 3.3. Статические методы (Static Methods)

`static`-методы принадлежат классу, а не объекту.  
Обычно используются для утилит, например, математических функций:

```java
class MathUtils {
    static int square(int x) {
        return x * x;
    }
}

System.out.println(MathUtils.square(5)); // 25
```

Статические методы можно сравнить с обычными функциями в других языках программирования, которые не привязаны к объекту.
В java нельзя объявить метод вне класса, поэтому все функции должны принадлежать какому-то классу.

Классы, в которых есть только статические методы, называют **утилитарными (utility class)**. 
Рекомендуется делать конструктор таких классов private, чтобы предотвратить случайное создание экземпляра и подчеркнуть предназначение класса:

```java
class MathUtils {
    private MathUtils() {
    }
  
    static int square(int x) {
        return x * x;
    }
}
```

#### Вызовы static и non-static методов

- **Из нестатического метода** можно вызвать статический напрямую: у экземпляра всегда есть доступ к классу.
- **Из статического метода** нельзя вызвать нестатический напрямую: у класса нет `this` и нет ссылки на объект.

```java
class SomeClass {
    static void f() {
        g(); // CE: требуется экземпляр
    }

    void g() {
        f(); // OK, то же самое, что SomeClass.f()
    }
}
```

Здесь:
- `f()` — статический метод, у него нет контекста объекта, поэтому `g()` вызвать напрямую нельзя.
- `g()` — экземплярный метод, он может вызвать `f()`, потому что статический метод доступен через сам класс.

Как правильно вызвать non-static из static:

```java
class SomeClass {
    static void f() {
        SomeClass obj = new SomeClass();
        obj.g(); // OK: вызов g() на конкретном объекте
    }

    void g() {
        System.out.println("Hello from g()");
    }
}
```

📌 Вывод:
- Статический метод не имеет доступа к `this`.
- Чтобы вызвать нестатический метод из `static`, нужно создать объект.

---

### 3.4. Статический блок инициализации (static initialization block)

Для сложной инициализации `static`-данных используют блок инициализации:

```java
class CalendarData {
    static final String[] days = new String[7];

    static {
        days[0] = "Monday";
        days[1] = "Tuesday";
        days[2] = "Wednesday";
        days[3] = "Thursday";
        days[4] = "Friday";
        days[5] = "Saturday";
        days[6] = "Sunday";
    }
}
```

Блок выполняется **один раз при загрузке класса**.  
Константные данные принято объявлять как `static final`.

---

### 3.5. Статические методы (Static factory methods)

Иногда для создания объектов используют `static` методы вместо конструкторов.  
Такие методы еще называют **фабричными методами (factory methods)**.  
Они инкапсулируют логику создания и позволяют больше гибкости, чем обычные конструкторы.

```java
class Person {
    private String name;

    private Person(String name) {
        this.name = name;
    }

    // фабричный метод
    static Person of(String name) {
        return new Person(name);
    }

    // ещё один фабричный метод
    static Person anonymous() {
        return new Person("Anonymous");
    }
}
```

Теперь:

```java
Person alice = Person.of("Alice");     // создаём обычного человека
Person guest = Person.anonymous();     // создаём "анонимного" пользователя
Person newPerson = new Person("bob");  // CE: конструктор приватный
```

Таким образом фабричные методы позволяют:
- ограничить создание объектов через `new`,
- кешировать и переиспользовать экземпляры,
- возвращать подтипы или заранее подготовленные объекты,
- можно использовать говорящие имена (of, from, copyOf, anonymous) вместо перегрузки конструкторов.

---

<details>
  <summary> 📒 Вопросы для самопроверки</summary>
  <p>1. В чём разница между обычным и `static` полем?</p>
  <p>2. Как `static` поле помогает посчитать количество созданных объектов?</p>
  <p>3. Чем отличаются `static` и instance методы?</p>
  <p>4. Почему из `static` метода нельзя напрямую вызвать нестатический?</p>
  <p>5. Для чего используют `static` initialization block?</p>
  <p>6. Почему фабричный метод `valueOf()` иногда лучше, чем конструктор?</p>
</details>

## 4. Пакеты и импорты (Packages и Imports)

### 4.1. Зачем нужны пакеты

**Package** в Java — это способ группировать связанные классы и управлять доступом между ними.  
Обычно пакет соответствует структуре директорий.

Пример:

```java
package store;

class Product {
    String name;
    int price;

    // constructor, methods
}
```

```java
package store;

class Basket {
    void add(Product product) {
        // добавляем товар в корзину
    }
}
```

Оба класса лежат в одном пакете `store` и имеют доступ друг к другу без ограничений.

---

### 4.2. Доступ между пакетами

Если классы находятся в разных пакетах, правила доступа становятся важными:

```java
package data;

class Product {
    String name;
    int price;
}
```

```java
package store;

class Basket {
    void add(data.Product product) { // CE
        // нельзя использовать непубличный класс из другого пакета
    }
}
```

Чтобы класс был доступен извне, его нужно объявить `public`:

```java
package data;

public class Product {
    String name;
    int price;
}
```

Теперь `Basket` может использовать `Product`, но доступ к его полям всё ещё закрыт.  
Чтобы поля и методы были доступны, их тоже нужно сделать `public`.

---

### 4.3. Виды модификаторов доступов (Access Modifiers)

- `public` — видимость везде.
- `private` — только внутри класса.
- *package-private* (по умолчанию, без модификатора) — доступен только в рамках одного пакета.
- `protected` — доступ внутри пакета **и в наследниках** (будет подробно рассмотрен в следующих лекциях).

Эти правила работают как для классов, так и для полей/методов.

---

### 4.4. Импорты (Imports)

Чтобы не писать полное имя класса каждый раз (`data.Product`), используется `import`.

```java
package store;

import data.Product;

class Basket {
    void add(Product product) {
        System.out.println(product.name); // OK, если поле public
    }
}
```

---

### 4.5. Виды import

- **Single-type import**
  ```java
  import data.Product;
  ```

- **On-demand import** (все классы пакета):
  ```java
  import data.*;
  ```
  ⚠️ Минус такого подхода: мы «подтягиваем» весь пакет.  
  При совпадении имён классов легко не заметить, что используется **не тот класс**, который ожидался.  
  Поэтому в промышленном коде обычно предпочитают **single-type import**.

- **Static import** (импорт статических методов и констант):
  ```java
  import static java.lang.Math.PI;
  import static java.lang.Math.max;

  System.out.println(max(3, 5) + PI);
  ```

- **Static on-demand import**:
  ```java
  import static java.lang.Math.*;
  ```

---

### 4.6. Ошибки при import

- **Конфликт имён**

```java
import java.util.Date;
import java.sql.Date; // CE: reference to Date is ambiguous
```

- **Импорт непубличного класса**

```java
import com.secret.Hidden; // CE, если класс не public
```

---

### 4.7. Дополнительно: дефолтные импорты и полезные советы

- В Java автоматически импортируются:
    - весь пакет `java.lang.*` (например, `String`, `Math`, `Object` доступны всегда),
    - классы текущего пакета.

- Полезные советы:
    - избегать `import *`, чтобы избежать скрытых конфликтов и повысить читаемость;
    - использовать **single-type import** для явности;
    - `static import *` допустим в тестах (например, для `assertThat`, `assertEquals`), но не в бизнес-коде;
    - поддерживать порядок импортов (часто IDE сортирует их автоматически: `java.*`, `javax.*`, сторонние библиотеки, внутренние пакеты).

---

<details>
  <summary> 📒 Вопросы для самопроверки</summary>
  <p>1. Для чего нужны пакеты в Java?</p>
  <p>2. Чем отличается доступ к классу `public` и package-private?</p>
  <p>3. Какие существуют виды import?</p>
  <p>4. Чем опасен on-demand import (`import data.*;`)?</p>
  <p>5. Какие пакеты импортируются в Java автоматически?</p>
  <p>6. Что произойдёт при импорте двух классов с одинаковыми именами?</p>
</details>

## 5. Внутренние и вложенные классы (Nested Classes)

### 5.1. Статические вложенные классы (Static Nested Classes)

**Static nested class** — это вложенный класс, объявленный с модификатором `static`.  
Он ведёт себя почти как обычный top-level класс, но при этом находится в scope внешнего класса.

Особенность: **не хранит ссылки на экземпляр внешнего класса**.

```java
class Outer {
    static class Nested {
        void hello() {
            System.out.println("Hi!");
        }
    }
}

Outer.Nested n = new Outer.Nested();
n.hello(); // Hi!
```

**Когда использовать:**
- вспомогательные классы, которые нужны только внутри одного outer-класса,
- для группировки кода и повышения читаемости,
- если класс может жить без экземпляра outer (например, `Builder` для создания объектов).

---

### 5.2. Внутренние классы (Inner Classes)

**Inner class** (нестатический вложенный класс) — связан с экземпляром внешнего класса.  
Хранит **неявную ссылку на внешний объект** и имеет доступ ко всем его полям и методам, включая `private`.

```java
class Outer {
    int x = 10;

    class Inner {
        void print() {
            System.out.println(x); // доступ к полю внешнего класса
        }
    }
}

Outer o = new Outer();
Outer.Inner i = o.new Inner();
i.print(); // 10
```

**Когда использовать:**
- когда вложенному классу нужен доступ к состоянию outer,
- при реализации паттернов проектирования (например, `Iterator`, когда итератор хранит ссылку на коллекцию),
- для инкапсуляции логики, связанной с конкретным экземпляром outer.

---

### 5.3. Локальные классы (Local Classes)

**Local class** объявляется внутри метода.  
Обычно используется как вспомогательная конструкция.

```java
class Outer {
    void process() {
        class LocalHelper {
            void run() {
                System.out.println("Running");
            }
        }
        new LocalHelper().run();
    }
}
```

Local class может обращаться к переменным метода, но только если они `final` или **effectively final** (не изменяются после присваивания).

**Когда использовать:**
- временные, одноразовые классы для помощи внутри метода,
- инкапсуляция сложной логики, которую не хочется выносить наружу,
- подготовка объектов для короткоживущих задач (например, обработка данных в одном методе).

---

### 5.4. Сравнение видов внутренних и вложенных классов

| Вид вложенного класса | Ссылка на внешний объект | Доступ к полям outer | Где объявляется | Практическое применение |
|------------------------|--------------------------|----------------------|-----------------|--------------------------|
| **Static nested**      | ❌ нет                   | ❌ нет               | Внутри класса, с `static` | Builder, вспомогательные структуры, логическая группировка |
| **Inner**              | ✅ есть                  | ✅ полный доступ      | Внутри класса, без `static` | Iterator, Listener, связанная логика с outer |
| **Local**              | зависят от scope         | ✅ к final/effectively final | Внутри метода | одноразовые вспомогательные классы внутри метода |

### 5.5. Практический пример: UserInfo

Рассмотрим класс `UserInfo`, в котором могут понадобиться вложенные классы:

```java
public class UserInfo {
    private String name;
    private int age;

    // static nested class — не зависит от экземпляра UserInfo
    public static class Address {
        private String city;
        private String street;
    }

    // inner class — связан с конкретным экземпляром UserInfo
    public class Session {
        private String token;

        public void invalidate() {
            // имеем доступ к полям UserInfo через неявную ссылку this.outer
            System.out.println("Invalidate for user: " + name);
        }
    }
    
    //getter'ы
    //setter'ы
}
```

- `Address` — **static nested class**: описывает адрес пользователя, но сам по себе не требует доступа к конкретному объекту `UserInfo`.  
  Такой класс может создаваться и использоваться независимо:
  ```java
  UserInfo.Address addr = new UserInfo.Address();
  ```

- `Session` — **inner class**: работает только в связке с конкретным пользователем и использует его поля.  
  Создаётся через экземпляр outer-класса:
  ```java
  UserInfo user = new UserInfo("Alice", 25);
  UserInfo.Session session = user.new Session();
  ```

📌 Практическое правило:
- Если вложенный класс **логически относится** к outer, но не зависит от его состояния → делаем `static nested`.
- Если вложенный класс **должен иметь доступ к полям экземпляра outer** → делаем `inner`.
- В промышленном коде предпочтение обычно отдают `static nested`, чтобы избежать лишних ссылок на outer и возможных утечек памяти.
---

<details>
  <summary> 📒 Вопросы для самопроверки</summary>
  <p>1. Чем отличается static nested class от inner class?</p>
  <p>2. Почему inner class имеет доступ к private-полям outer-класса?</p>
  <p>3. Как создаётся экземпляр inner class?</p>
  <p>4. Какие ограничения есть у local class в отношении переменных метода?</p>
  <p>5. Когда стоит использовать static nested class?</p>
  <p>6. Для чего может пригодиться inner class в коллекциях?</p>
  <p>7. Почему local class полезен при одноразовой обработке внутри метода?</p>
</details>